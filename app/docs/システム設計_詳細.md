# Open Regime 新システム設計（詳細版）

> 作成日: 2026-02-19
> このドキュメントは設計議論の結果をまとめたものです

---

## 1. 現状の問題点（demoシステム）

### 1.1 技術的な問題

| 現状 | 問題点 |
|------|--------|
| Flask + Jinja2 | サーバーで毎回HTML生成、遅い |
| SQLite | ファイルベース、同時書き込み不可 |
| ローカル実行 | 自分のPCでしか動かない |
| yfinance毎回呼び出し | レート制限、遅い |

### 1.2 現状のDB構成（ごちゃ混ぜ）

`demo/data/market_data.db` に21テーブルが混在：

```
流動性・金融システム（7テーブル）
├── fed_balance_sheet  (3,455件)
├── interest_rates     (4,350件)
├── credit_spreads     (4,469件)
├── srf_usage          (1,619件)
├── bank_sector        (4,290件)
├── margin_debt        (348件)
└── mmf_assets         (63件)

市場全体指標（3テーブル）
├── market_indicators    (4,293件)
├── market_state_history (192件)
└── layer_stress_history (576件)

雇用・経済指標（3テーブル）
├── employment_monthly (36件)
├── weekly_claims      (103件)
└── employment_alerts  (1件)

株式・シグナル（5テーブル）
├── trade_signals      (5,794件) ※重複あり
├── signal_snapshots   (1,215件)
├── signal_outcomes    (1,215件)
├── stock_analysis     (321件)
└── stock_prices       (0件) ※未使用

アラート・ログ（1テーブル）
└── alerts_log (0件) ※未使用
```

### 1.3 現状のキャッシュ

- **場所**: Pythonのメモリ（RAM）
- **TTL**: 5分
- **問題**: サーバー再起動で消える、DBに保存していない

### 1.4 現状の銘柄管理

- ハードコードされている
- DBに銘柄マスターがない

---

## 2. 新システムのアーキテクチャ

### 2.1 全体構成

```
┌─────────────────────────────────────────────────────────────┐
│                    ユーザー（ブラウザ・日本）                │
└───────────────────────────┬─────────────────────────────────┘
                            │ HTTPS (~10ms)
                            ↓
┌─────────────────────────────────────────────────────────────┐
│           Cloudflare Pages（無料・Edge配信）                 │
│           URL: https://open-regime.pages.dev                │
│           + Supabase Auth（Email/Password + Google OAuth）   │
│                                                              │
│  Next.js 15.2 + TypeScript + Tailwind CSS + shadcn/ui       │
│                                                              │
│  役割: UI表示のみ（SSG静的配信）                            │
│  ├── チャート描画（Canvas: ローソク足/ライン）              │
│  ├── SWR（ブラウザキャッシュ: 2回目以降0ms）               │
│  └── 計算ロジックは一切置かない                             │
└───────────────────────────┬─────────────────────────────────┘
                            │ fetch() API呼び出し (~60ms on HIT)
                            ↓
┌─────────────────────────────────────────────────────────────┐
│       Cloudflare Worker — Edge APIプロキシ（無料）          │
│       URL: https://open-regime-api.ryu3ta-ke-mo100307       │
│            .workers.dev                                      │
│                                                              │
│  役割: APIキャッシュプロキシ（東京PoP: NRT）               │
│  ├── Cache API でレスポンスをEdgeキャッシュ                 │
│  ├── エンドポイント別TTL（5分〜4時間）                     │
│  ├── キャッシュHIT → 即返却（~60ms）                       │
│  ├── キャッシュMISS → CF backbone経由でRailwayへ           │
│  ├── holdings/trades/signal → キャッシュなし（パススルー）  │
│  └── CORSヘッダー + X-Cache: HIT/MISS ヘッダー            │
│                                                              │
│  コード: app/worker/src/index.ts                            │
│  設定:   app/worker/wrangler.jsonc                          │
└───────────────────────────┬─────────────────────────────────┘
                            │ CF backbone (~300ms on MISS)
                            ↓
┌─────────────────────────────────────────────────────────────┐
│           Railway（無料枠 $5/月・US East Virginia）         │
│           URL: https://empathetic-hope-production           │
│                .up.railway.app                               │
│                                                              │
│  FastAPI + Python                                           │
│                                                              │
│  役割: 全ての計算ロジック（外から見えない）                 │
│  ├── combined_entry_detector.py（V10）                      │
│  ├── exit_manager.py / regime_detector.py                   │
│  ├── choch_detector.py / bos_detector.py                    │
│  └── 事前計算結果をDB直読み（precomputed_results）         │
│                                                              │
│  APIエンドポイント（25+個）                                 │
│  ※ V10ロジック等は完全にサーバー内、外部から見えない       │
└───────────────────────────┬─────────────────────────────────┘
                            │
              ┌─────────────┴─────────────┐
              ↓                           ↓
┌─────────────────────────┐   ┌─────────────────────────────┐
│   Supabase（無料）      │   │      yfinance API           │
│   PostgreSQL            │   │   株価データ取得            │
│   RLS有効（全テーブル） │   │   （都度取得、DB保存なし）  │
│   service_roleキー      │   │                             │
└─────────────────────────┘   └─────────────────────────────┘

              ▲ daily/weekly バッチ
┌─────────────────────────────────────────────────────────────┐
│  app/batch/run.py                                           │
│  FRED・Yahoo・NY Fed → Supabase upsert                     │
│  → precompute_all() → _warmup_cache()                      │
│    （Worker全エンドポイントを1回叩いてキャッシュ充填）      │
└─────────────────────────────────────────────────────────────┘
```

### 2.2 なぜこの構成か

| 要件 | 解決策 |
|------|--------|
| 計算ロジックを隠したい | FastAPIをサーバー側に置く（フロントに露出しない） |
| Flaskを使いたくない | FastAPI（モダン、高速、型安全） |
| 複数人がアクセス | クラウドホスティング |
| 無料で運用 | Cloudflare Pages + Railway + Supabase（全て無料枠） |

### 2.3 ロジック保護

```
ユーザーが見えるもの:
├── Next.js のフロントエンドコード（UI描画のみ）
├── APIリクエスト/レスポンス（JSON）
└── 結果の数値（シグナルあり/なし等）

ユーザーが見えないもの:
├── combined_entry_detector.py の中身
├── EMA収束の閾値（1.3, 1.0, 0.8, 2.0）
├── RS DOWN閾値の株価カテゴリ別設定
├── CHoCH検出ロジック
└── 全てのPythonコード
```

---

## 3. データベース設計

### 3.1 設計方針

**新システムに持ち込むタブ:**
- 統合タブ
- 配管タブ
- 米国景気警戒タブ
- シグナルタブ
- 保有タブ

**保存するもの:**
- 銘柄マスター（カテゴリ、名前等）
- 流動性データ（FRBバランスシート、金利、クレジットスプレッド等）
- 経済指標（修正履歴対応・縦持ち方式で無限追加可能）
- 市場状態履歴（配管・統合タブ用）
- 個人データ（保有・取引・ウォッチリスト・設定）
- 一時キャッシュ（5分TTL、複数ユーザー共有）

**保存しないもの:**
- 株価OHLCV → yfinanceで都度取得
- リアルタイム価格 → yfinanceで都度取得
- EMA/ATR等 → 株価から都度計算
- シグナル結果 → Railwayで都度計算（DB保存不要）

### 3.2 テーブル一覧（22テーブル）

```
┌─────────────────────────────────────────────────────────────┐
│                     Supabase PostgreSQL                      │
├─────────────────────────────────────────────────────────────┤
│                                                              │
│  【認証基盤】（1テーブル）★2026-02-28追加                    │
│  └── users                    UUID認証・商用化対応            │
│                                                              │
│  【シグナル分析用】（2テーブル）                             │
│  ├── stock_master              銘柄マスター                  │
│  └── stock_cache               一時キャッシュ（5分TTL）       │
│                                                              │
│  【配管タブ用 - 流動性グループ】（8テーブル）                │
│  ├── fed_balance_sheet         FRBバランスシート              │
│  ├── interest_rates            金利                          │
│  ├── credit_spreads            クレジットスプレッド           │
│  ├── market_indicators         VIX, DXY, SP500, NASDAQ       │
│  ├── bank_sector               KRE                           │
│  ├── srf_usage                 SRF利用額                     │
│  ├── margin_debt               信用取引残高                   │
│  └── mmf_assets                MMF資産                       │
│                                                              │
│  【米国景気警戒タブ用 - 雇用グループ】（3テーブル）          │
│  ├── economic_indicators       経済指標マスター               │
│  ├── economic_indicator_revisions  修正履歴（無限追加可能）   │
│  └── weekly_claims             週次失業保険                   │
│                                                              │
│  【統合タブ用 - 履歴グループ】（2テーブル）                  │
│  ├── market_state_history      マーケット状態履歴             │
│  └── layer_stress_history      レイヤーストレス履歴           │
│                                                              │
│  【保有タブ用 - 個人データ】（6テーブル）（RLS適用）         │
│  ├── holdings                  保有銘柄（user_id=UUID）       │
│  ├── trades                    取引履歴（user_id=UUID）       │
│  ├── cash_balances             現金・預金残高 ★2026-02-25追加 │
│  ├── portfolio_snapshots       日次ポートフォリオ ★2026-02-25 │
│  ├── user_watchlists           ウォッチリスト                 │
│  └── user_settings             ユーザー設定                   │
│                                                              │
└─────────────────────────────────────────────────────────────┘
```

### 3.3 demoから移行しないテーブル（削除）

| テーブル | 理由 |
|----------|------|
| signal_snapshots | バックテスト専用（都度計算で対応） |
| signal_outcomes | バックテスト専用（都度計算で対応） |
| trade_signals | 古いシステムの遺物（都度計算で対応） |
| stock_analysis | 古いシステムの遺物（都度計算で対応） |
| stock_prices | 未使用（0件） |
| alerts_log | 未使用（0件） |
| employment_alerts | 統合タブで代替 |
| liquidity.db | 空ファイル |

### 3.4 テーブル定義（SQL）

```sql
-- =====================================================
-- 【シグナル分析用】
-- =====================================================

-- 銘柄マスター
CREATE TABLE stock_master (
    ticker VARCHAR(10) PRIMARY KEY,
    name VARCHAR(100),                  -- 'NVIDIA Corporation'
    sector VARCHAR(50),                 -- 'Technology'
    industry VARCHAR(100),              -- 'Semiconductors'

    -- カテゴリ
    price_category VARCHAR(20),         -- 'penny', 'mid', 'large'
    watchlist_category VARCHAR(50),     -- 'robotics', 'defense', 'ai'

    -- メタ情報
    market_cap BIGINT,
    exchange VARCHAR(10),               -- 'NASDAQ', 'NYSE'

    -- 状態
    is_active BOOLEAN DEFAULT TRUE,
    added_at TIMESTAMPTZ DEFAULT NOW(),
    updated_at TIMESTAMPTZ DEFAULT NOW()
);

-- 一時キャッシュ（5分TTL）
CREATE TABLE stock_cache (
    ticker VARCHAR(10) PRIMARY KEY,
    data JSONB,                         -- OHLCV + 分析結果
    fetched_at TIMESTAMPTZ DEFAULT NOW(),
    expires_at TIMESTAMPTZ DEFAULT NOW() + INTERVAL '5 minutes'
);

CREATE INDEX idx_stock_cache_expires ON stock_cache(expires_at);

-- =====================================================
-- 【配管タブ用 - 流動性グループ】
-- =====================================================

-- FRBバランスシート
CREATE TABLE fed_balance_sheet (
    date DATE PRIMARY KEY,
    reserves DECIMAL,                   -- 準備預金（10億ドル）
    rrp DECIMAL,                        -- リバースレポ（10億ドル）
    tga DECIMAL,                        -- 財務省当座預金（10億ドル）
    soma_assets DECIMAL,                -- SOMA資産合計（10億ドル）
    updated_at TIMESTAMPTZ DEFAULT NOW()
);

-- 金利
CREATE TABLE interest_rates (
    date DATE PRIMARY KEY,
    fed_funds DECIMAL,                  -- FFレート
    treasury_2y DECIMAL,                -- 2年国債利回り
    treasury_10y DECIMAL,               -- 10年国債利回り
    treasury_spread DECIMAL,            -- 10年-2年スプレッド
    updated_at TIMESTAMPTZ DEFAULT NOW()
);

-- クレジットスプレッド
CREATE TABLE credit_spreads (
    date DATE PRIMARY KEY,
    hy_spread DECIMAL,                  -- ハイイールドスプレッド
    ig_spread DECIMAL,                  -- 投資適格スプレッド
    ted_spread DECIMAL,                 -- TEDスプレッド
    updated_at TIMESTAMPTZ DEFAULT NOW()
);

-- 市場指標
CREATE TABLE market_indicators (
    date DATE PRIMARY KEY,
    vix DECIMAL,                        -- VIX指数
    dxy DECIMAL,                        -- ドルインデックス
    sp500 DECIMAL,                      -- S&P500
    nasdaq DECIMAL,                     -- NASDAQ
    updated_at TIMESTAMPTZ DEFAULT NOW()
);

-- 銀行セクター（KRE）
CREATE TABLE bank_sector (
    date DATE PRIMARY KEY,
    kre_close DECIMAL,                  -- KRE終値
    kre_52w_high DECIMAL,               -- 52週高値
    kre_52w_low DECIMAL,                -- 52週安値
    kre_52w_change DECIMAL,             -- 52週変化率（%）
    updated_at TIMESTAMPTZ DEFAULT NOW()
);

-- SRF利用額
CREATE TABLE srf_usage (
    date DATE PRIMARY KEY,
    amount DECIMAL,                     -- 利用額（10億ドル）
    source VARCHAR(20),                 -- 'manual' or 'h41'
    created_at TIMESTAMPTZ DEFAULT NOW(),
    updated_at TIMESTAMPTZ DEFAULT NOW()
);

-- 信用取引残高
CREATE TABLE margin_debt (
    date DATE PRIMARY KEY,
    debit_balance DECIMAL,              -- 信用取引残高（10億ドル）
    free_credit DECIMAL,                -- 無担保信用残高
    change_2y DECIMAL,                  -- 2年変化率（%）
    updated_at TIMESTAMPTZ DEFAULT NOW()
);

-- MMF資産
CREATE TABLE mmf_assets (
    date DATE PRIMARY KEY,
    total_assets DECIMAL,               -- 総資産（10億ドル）
    change_3m DECIMAL,                  -- 3ヶ月変化率（%）
    updated_at TIMESTAMPTZ DEFAULT NOW()
);

-- =====================================================
-- 【米国景気警戒タブ用 - 雇用グループ】
-- =====================================================

-- 経済指標マスター（修正履歴対応・縦持ち方式）
CREATE TABLE economic_indicators (
    id SERIAL PRIMARY KEY,
    indicator VARCHAR(50) NOT NULL,     -- 'NFP', 'GDP', 'CPI', 'UNEMPLOYMENT', etc.
    reference_period DATE NOT NULL,     -- 対象期間（2026-01-01 = 2026年1月分）
    current_value DECIMAL,              -- 最新の値
    revision_count INTEGER DEFAULT 0,   -- 修正回数

    -- 追加の雇用統計詳細（NFP用）
    nfp_change INTEGER,                 -- 前月比変化（千人）
    u3_rate DECIMAL,                    -- 公式失業率 U3（%）
    u6_rate DECIMAL,                    -- 実質失業率 U6（%）
    avg_hourly_earnings DECIMAL,        -- 平均時給（ドル）
    wage_mom DECIMAL,                   -- 平均時給 前月比（%）
    labor_force_participation DECIMAL,  -- 労働参加率（%）

    notes TEXT,
    created_at TIMESTAMPTZ DEFAULT NOW(),
    updated_at TIMESTAMPTZ DEFAULT NOW(),

    UNIQUE(indicator, reference_period)
);

-- 経済指標の修正履歴（無限追加可能）
CREATE TABLE economic_indicator_revisions (
    id SERIAL PRIMARY KEY,
    indicator_id INTEGER REFERENCES economic_indicators(id) ON DELETE CASCADE,
    revision_number INTEGER NOT NULL,   -- 0=速報, 1=第1次改定, 2=第2次改定, ...
    value DECIMAL NOT NULL,
    published_date DATE NOT NULL,       -- 発表日
    change_from_prev DECIMAL,           -- 前回発表値からの変化
    change_pct_from_prev DECIMAL,       -- 前回発表値からの変化率（%）
    notes TEXT,
    created_at TIMESTAMPTZ DEFAULT NOW(),

    UNIQUE(indicator_id, revision_number)
);

CREATE INDEX idx_revisions_indicator ON economic_indicator_revisions(indicator_id);
CREATE INDEX idx_revisions_date ON economic_indicator_revisions(published_date);

-- 週次失業保険
CREATE TABLE weekly_claims (
    week_ending DATE PRIMARY KEY,       -- 週末日（YYYY-MM-DD）
    initial_claims INTEGER,             -- 新規失業保険申請件数
    continued_claims INTEGER,           -- 継続失業保険申請件数
    initial_claims_4w_avg INTEGER,      -- 新規申請4週移動平均
    created_at TIMESTAMPTZ DEFAULT NOW(),
    updated_at TIMESTAMPTZ DEFAULT NOW()
);

-- =====================================================
-- 【統合タブ用 - 履歴グループ】
-- =====================================================

-- マーケット状態履歴
CREATE TABLE market_state_history (
    id SERIAL PRIMARY KEY,
    date DATE NOT NULL UNIQUE,
    state VARCHAR(50) NOT NULL,         -- 'HEALTHY', 'MARKET_OVERSHOOT', etc.
    layer1_stress DECIMAL,
    layer2a_stress DECIMAL,
    layer2b_stress DECIMAL,
    credit_pressure VARCHAR(20),        -- 'Low', 'Medium', 'High'
    comment TEXT,
    created_at TIMESTAMPTZ DEFAULT NOW()
);

-- レイヤーストレス履歴
CREATE TABLE layer_stress_history (
    id SERIAL PRIMARY KEY,
    date DATE NOT NULL,
    layer VARCHAR(20) NOT NULL,         -- 'layer1', 'layer2a', 'layer2b'
    stress_score DECIMAL NOT NULL,
    components JSONB,                   -- 詳細をJSON形式で保存
    created_at TIMESTAMPTZ DEFAULT NOW(),

    UNIQUE(date, layer)
);

-- =====================================================
-- 【保有タブ用 - 個人データ】
-- =====================================================

-- 保有銘柄
CREATE TABLE holdings (
    id UUID DEFAULT gen_random_uuid() PRIMARY KEY,
    user_id TEXT NOT NULL,  -- Cloudflare Access メールアドレス

    ticker VARCHAR(10) NOT NULL,
    shares DECIMAL(12,4) NOT NULL,
    avg_price DECIMAL(12,4) NOT NULL,

    -- 取引情報
    entry_date DATE,
    account_type VARCHAR(20) DEFAULT 'tokutei',  -- 'nisa', 'tokutei'
    sector VARCHAR(50),                 -- 'Technology', 'Finance', etc.

    -- エントリー時の状態（V10用）
    regime_at_entry VARCHAR(20),        -- 'BULL', 'BEAR', 'RECOVERY', 'WEAKENING'
    rs_at_entry VARCHAR(20),            -- 'UP', 'FLAT', 'DOWN'
    fx_rate DECIMAL(8,4) DEFAULT 150.0, -- 為替レート

    -- 管理情報
    target_price DECIMAL(12,4),
    stop_loss DECIMAL(12,4),
    thesis TEXT,                        -- 投資理由
    notes TEXT,

    created_at TIMESTAMPTZ DEFAULT NOW(),
    updated_at TIMESTAMPTZ DEFAULT NOW()
);

ALTER TABLE holdings ENABLE ROW LEVEL SECURITY;
-- 注: service_role キーで接続しているため RLS はバイパスされる
-- データ分離はアプリケーションコード側で .eq("user_id", user_email) で実施

-- 取引履歴
CREATE TABLE trades (
    id UUID DEFAULT gen_random_uuid() PRIMARY KEY,
    user_id TEXT NOT NULL,  -- Cloudflare Access メールアドレス
    holding_id UUID REFERENCES holdings(id) ON DELETE SET NULL,

    ticker VARCHAR(10) NOT NULL,
    action VARCHAR(10) NOT NULL,        -- 'BUY', 'SELL'
    shares DECIMAL(12,4) NOT NULL,
    price DECIMAL(12,4) NOT NULL,
    fees DECIMAL(10,4) DEFAULT 0,

    trade_date TIMESTAMPTZ NOT NULL,
    account_type VARCHAR(20),

    -- エントリー/エグジット時の状態
    regime VARCHAR(20),                 -- 取引時のMarket Regime
    rs_trend VARCHAR(20),               -- 取引時のRS Trend

    -- 振り返り
    reason TEXT,                        -- entry_reason / exit_reason
    lessons_learned TEXT,

    -- 結果（SELL時）
    profit_loss DECIMAL(12,4),
    profit_loss_pct DECIMAL(8,4),
    holding_days INTEGER,

    created_at TIMESTAMPTZ DEFAULT NOW()
);

ALTER TABLE trades ENABLE ROW LEVEL SECURITY;
-- 注: service_role キーで接続しているため RLS はバイパスされる

-- =====================================================
-- ウォッチリスト（個人データ）
-- =====================================================
CREATE TABLE user_watchlists (
    id UUID DEFAULT gen_random_uuid() PRIMARY KEY,
    user_id TEXT NOT NULL,  -- Cloudflare Access メールアドレス

    name VARCHAR(50) NOT NULL,          -- 'メイン', 'ロボティクス'
    tickers TEXT[] NOT NULL,            -- ['NVDA', 'TSLA', 'PLTR']
    is_default BOOLEAN DEFAULT FALSE,

    created_at TIMESTAMPTZ DEFAULT NOW(),
    updated_at TIMESTAMPTZ DEFAULT NOW()
);

ALTER TABLE user_watchlists ENABLE ROW LEVEL SECURITY;
-- 注: service_role キーで接続しているため RLS はバイパスされる

-- =====================================================
-- ユーザー設定（個人データ）
-- =====================================================
CREATE TABLE user_settings (
    id UUID DEFAULT gen_random_uuid() PRIMARY KEY,
    user_id TEXT UNIQUE NOT NULL,  -- Cloudflare Access メールアドレス

    display_name VARCHAR(50),
    default_account_type VARCHAR(20) DEFAULT 'tokutei',

    -- 表示設定
    theme VARCHAR(20) DEFAULT 'dark',
    default_chart_period VARCHAR(10) DEFAULT '3mo',

    -- 運用モード
    trading_mode VARCHAR(20) DEFAULT 'balanced',  -- 'aggressive', 'balanced', 'conservative'

    created_at TIMESTAMPTZ DEFAULT NOW(),
    updated_at TIMESTAMPTZ DEFAULT NOW()
);

ALTER TABLE user_settings ENABLE ROW LEVEL SECURITY;
-- 注: service_role キーで接続しているため RLS はバイパスされる

-- =====================================================
-- 現金・預金残高（2026-02-25追加）
-- =====================================================
CREATE TABLE cash_balances (
    id UUID DEFAULT gen_random_uuid() PRIMARY KEY,
    user_id TEXT NOT NULL,

    label VARCHAR(100) NOT NULL,            -- 'SBI証券', '楽天証券' 等
    currency VARCHAR(10) NOT NULL DEFAULT 'JPY',  -- 'JPY', 'USD'
    amount DECIMAL(14,2) NOT NULL DEFAULT 0,
    account_type VARCHAR(20),               -- 'nisa', 'tokutei', 'bank'

    updated_at TIMESTAMPTZ DEFAULT NOW()
);

CREATE INDEX idx_cash_user ON cash_balances(user_id);

-- =====================================================
-- 日次ポートフォリオスナップショット（2026-02-25追加）
-- =====================================================
-- 毎日の終値でポートフォリオを時価評価し保存。
-- 資産推移チャートのデータソース。
-- バッチ日次処理（app/batch/snapshot.py）で自動生成。
CREATE TABLE portfolio_snapshots (
    id UUID DEFAULT gen_random_uuid() PRIMARY KEY,
    user_id TEXT NOT NULL,
    snapshot_date DATE NOT NULL,

    total_market_value_usd DECIMAL NOT NULL,  -- Σ(shares × close_price)
    total_cost_usd DECIMAL NOT NULL,          -- Σ(shares × avg_price)
    unrealized_pnl_usd DECIMAL NOT NULL,      -- market_value - cost
    cash_usd DECIMAL NOT NULL DEFAULT 0,      -- JPY現金をUSD換算 + USD現金
    total_assets_usd DECIMAL NOT NULL DEFAULT 0, -- market_value + cash
    fx_rate_usdjpy DECIMAL,                   -- 換算に使ったレート
    holdings_count INTEGER NOT NULL DEFAULT 0,
    holdings_detail JSONB,                    -- 銘柄別詳細（ticker, shares, close_price等）

    created_at TIMESTAMPTZ DEFAULT NOW(),
    CONSTRAINT uq_snapshot UNIQUE (user_id, snapshot_date)
);

CREATE INDEX idx_snapshots_user_date
    ON portfolio_snapshots (user_id, snapshot_date DESC);

-- =====================================================
-- updated_at 自動更新トリガー
-- =====================================================
CREATE OR REPLACE FUNCTION update_updated_at()
RETURNS TRIGGER AS $$
BEGIN
    NEW.updated_at = NOW();
    RETURN NEW;
END;
$$ LANGUAGE plpgsql;

CREATE TRIGGER stock_master_updated_at
    BEFORE UPDATE ON stock_master
    FOR EACH ROW EXECUTE FUNCTION update_updated_at();

CREATE TRIGGER holdings_updated_at
    BEFORE UPDATE ON holdings
    FOR EACH ROW EXECUTE FUNCTION update_updated_at();

CREATE TRIGGER user_watchlists_updated_at
    BEFORE UPDATE ON user_watchlists
    FOR EACH ROW EXECUTE FUNCTION update_updated_at();

CREATE TRIGGER user_settings_updated_at
    BEFORE UPDATE ON user_settings
    FOR EACH ROW EXECUTE FUNCTION update_updated_at();
```

---

## 4. キャッシュ戦略

### 4.1 なぜキャッシュが必要か

```
問題: yfinanceにはレート制限がある

ユーザーA → NVDAを見る → yfinance API
ユーザーB → NVDAを見る → yfinance API  ← 無駄！

解決: 一時キャッシュ（5分TTL）

ユーザーA → NVDAを見る → yfinance API → DBに一時保存
ユーザーB → NVDAを見る → DBから返す（API呼ばない）
5分後 → 自動削除
```

### 4.2 キャッシュのライフサイクル

```
1. ユーザーが銘柄を要求
2. DBのstock_cacheを確認
   ├── あり＆有効期限内 → DBから返す
   └── なし or 期限切れ → yfinance API呼び出し → DBに保存 → 返す
3. 定期的に期限切れを削除（cron or GitHub Actions）
```

### 4.3 キャッシュクリーンアップ

```sql
-- 5分ごとに実行
DELETE FROM stock_cache WHERE expires_at < NOW();
```

---

## 5. API設計

### 5.1 FastAPI エンドポイント

```
/api/
├── analyze/
│   └── GET /{ticker}           シグナル分析（V10ロジック）
│       Query: mode=balanced|aggressive|conservative
│       Response: { entry_allowed, conditions, relative_strength, ... }
│
├── exit/
│   └── GET /{ticker}           Exit判定
│       Query: entry_price, entry_date
│       Response: { mirror, heat_score, trail_stop, ... }
│
├── regime/
│   └── GET /                   市場レジーム
│       Response: { regime, spy_above_200ema, spy_ema21_slope, ... }
│
├── stock/
│   └── GET /{ticker}           株価データ（キャッシュ経由）
│       Query: period=3mo, interval=1d
│       Response: { dates, opens, highs, lows, closes, volumes, ... }
│
└── chart/
    └── GET /{ticker}/signals   チャート用シグナル（BOS/CHoCH/FVG）
        Response: { bos_list, choch_list, fvg_list, ... }
```

### 5.2 認証（2026-02-28更新）

#### 一般ユーザー認証: Supabase Auth

- **Supabase Auth** で Email/Password + Google OAuth 認証
- フロントエンドで `@supabase/supabase-js` を使用（`flowType: 'implicit'`、静的エクスポート対応）
- ログイン後 `Authorization: Bearer <JWT>` ヘッダーで API 呼び出し
- Worker が信頼 Origin チェック後 Backend に `Authorization` ヘッダーを転送
- Backend `require_auth()` が JWT を検証し、`sub` + `email` からユーザー解決
  - **ES256 (ECDSA)**: JWKS 公開鍵で検証（`PyJWKClient` → `{SUPABASE_URL}/auth/v1/.well-known/jwks.json`、1時間キャッシュ）
  - **HS256 フォールバック**: `SUPABASE_JWT_SECRET` で HMAC 検証（レガシー互換）
  - JWT ヘッダーの `alg` フィールドで自動判定（Supabase はデフォルト ES256）
- 既存の `X-User-Email` + `X-Proxy-Secret` は移行期間中のフォールバックとして残存
- 初回ログイン時にユーザーを自動作成、CF Access 時代のユーザーは email で自動リンク
- **`require_auth` は `users` テーブルの UUID を返す**（メールアドレスではない）
- holdings/trades は `user_id` = UUID（`users.id`）でユーザーデータ分離
- `GET /api/me` で現在のユーザー情報（UUID, email, plan 等）を取得可能

#### 認証ページ

| ページ | ファイル | 内容 |
|--------|---------|------|
| ログイン | `app/frontend/src/app/login/page.tsx` | Email+Password / Google OAuth / パスワード表示トグル |
| 新規登録 | `app/frontend/src/app/register/page.tsx` | Email+Password（強度要件5項目）/ Google OAuth / 利用規約同意 |
| パスワードリセット | `app/frontend/src/app/reset-password/page.tsx` | Email 入力 → リセットメール送信 |
| OAuth コールバック | `app/frontend/src/app/auth/callback/page.tsx` | Implicit flow ハッシュフラグメント処理 |
| メール確認待ち | `app/frontend/src/app/auth/verify/page.tsx` | 確認メール送信済み画面 + 再送ボタン |

#### パスワード要件

- 8文字以上、大文字(A-Z)、小文字(a-z)、数字(0-9)、記号(!@#$...) — 全5項目をリアルタイムチェック表示

#### AuthGuard（認証ガード）

- `AuthGuard.tsx` コンポーネントで保護ページをラップ
- 未認証ユーザーは `/login/` にリダイレクト
- 保護対象: `/dashboard`, `/liquidity`, `/employment`, `/signals`, `/holdings`, `/settings`
- 公開ページ: `/`, `/login`, `/register`, `/reset-password`, `/auth/*`

#### 管理者認証（変更なし）

- Cloudflare Access + ADMIN_EMAILS + TOTP MFA の3層構造を維持（セクション 19, 20 参照）

---

## 6. 技術スタック

| レイヤー | 技術 | 理由 |
|---------|------|------|
| フロントエンド | Next.js 15 + TypeScript | App Router、RSC対応 |
| スタイル | Tailwind CSS | 高速開発 |
| UIライブラリ | shadcn/ui | 高品質コンポーネント |
| チャート | Chart.js / Recharts | 既存資産活用 |
| ホスティング（フロント） | Cloudflare Pages | 無料、高速CDN |
| バックエンド | FastAPI + Python | 既存ロジック活用、ロジック保護 |
| ホスティング（バック） | Railway | 無料枠$5/月 |
| データベース | Supabase PostgreSQL | 無料枠500MB、Auth込み |
| 認証（一般） | Supabase Auth | Email/Password + Google OAuth (Implicit flow) |
| 認証（管理者） | Cloudflare Access + TOTP MFA | 3層認証（Zero Trust + ADMIN_EMAILS + TOTP） |
| 株価API | yfinance | 無料 |

---

## 7. コスト

| サービス | プラン | 月額 |
|---------|--------|------|
| Cloudflare Pages | Free | $0 |
| Cloudflare Workers | Free (100K req/day) | $0 |
| Railway | Free | $0（$5クレジット内） |
| Supabase | Free | $0 |
| GitHub Actions | Free (2000 min/month) | $0 |
| **合計** | | **$0** |

---

## 8. セキュリティ

### 8.1 ロジック保護

- 計算ロジック（V10等）はFastAPIサーバー内に配置
- フロントエンドには結果のJSONのみ返す
- ソースコードはサーバー外に露出しない

### 8.2 データ保護

- バックエンドは `service_role` キーで Supabase に接続（RLS バイパス）
- **全てのユーザーデータ分離はアプリケーションコード（`require_auth` + `.eq("user_id", uuid)`）で実施**
- 一般ユーザー: `Authorization: Bearer <JWT>` で認証（Supabase Auth 発行の JWT）
  - JWT 署名検証: ES256 → JWKS 公開鍵、HS256 → HMAC シークレット（`auth.py` で `alg` ヘッダーにより自動切替）
- 管理者: `X-User-Email` + Cloudflare Access + TOTP MFA で認証
- **401 ループ防止**: `auth-store.ts` の `_isRedirecting` フラグで、JWT 検証失敗→signOut→リダイレクトの無限ループを防止（SWR fetcher + fetchAPI 両方に適用）

### 8.3 API保護

- CORS設定でフロントエンドからのみアクセス許可
- レート制限（FastAPI）

---

## 9. 今後の検討事項

### 9.1 任意で追加検討

| 項目 | 説明 | 優先度 |
|------|------|--------|
| signal_log | Slack通知のログ | 低 |
| daily_market_data | 日次マーケットデータ統合 | 低 |
| stock_daily | 日次確定株価（バックテスト高速化） | 低 |

### 9.2 将来の拡張

- Slack通知
- モバイル対応
- 追加の分析機能

---

## 10. ディレクトリ構成（予定）

```
app/
├── docs/                           # ドキュメント
│   ├── システム設計_詳細.md        # このファイル
│   └── システム概要_簡易版.md      # 簡易版
│
├── backend/                        # FastAPI（Railway）
│   ├── main.py                     # エントリーポイント（CORS: Worker URL含む）
│   ├── auth.py                     # Cloudflare Access認証
│   ├── precomputed.py              # 事前計算結果の高速読み取り
│   ├── routers/                    # 10ルーター
│   ├── analysis/                   # 計算ロジック（demoから移植）
│   ├── requirements.txt
│   └── Dockerfile
│
├── frontend/                       # Next.js（Cloudflare Pages）
│   ├── .env.local                  # NEXT_PUBLIC_API_URL → Worker URL
│   ├── src/app/                    # ページ（signals, holdings, employment, liquidity）
│   ├── src/components/             # UI（charts/, shared/, ui/）
│   ├── src/lib/                    # api.ts（SWRフック15個）, swr.tsx
│   └── package.json
│
├── worker/                         # Cloudflare Worker（Edge APIプロキシ）
│   ├── wrangler.jsonc              # Worker設定（name, vars）
│   ├── package.json                # wrangler + typescript
│   ├── tsconfig.json
│   └── src/
│       ├── index.ts                # メインプロキシロジック
│       └── cache-config.ts         # エンドポイント別TTL設定
│
├── batch/                          # バッチデータ更新
│   ├── run.py                      # CLI（--daily, --weekly, --precompute）
│   ├── calculators/precompute.py   # 事前計算
│   ├── fetchers/                   # FRED, Yahoo, NY Fed
│   └── sql/                        # RLS設定SQL
│
└── .github/workflows/
    └── deploy.yml                  # CI/CD: Worker + Pages 自動デプロイ
```

---

## 7. データ移行

### 7.1 移行スクリプト

`app/scripts/migrate_to_supabase.py` で demo SQLite → Supabase への移行を実行。

### 7.2 移行結果（2026-02-19実行）

| テーブル | 件数 | 備考 |
|---------|------|------|
| fed_balance_sheet | 3,455 | 配管タブ |
| interest_rates | 4,350 | 配管タブ |
| credit_spreads | 4,469 | 配管タブ |
| market_indicators | 4,293 | 配管タブ |
| bank_sector | 4,290 | 配管タブ |
| srf_usage | 1,619 | 配管タブ |
| margin_debt | 348 | 配管タブ |
| mmf_assets | 63 | 配管タブ |
| weekly_claims | 103 | 米国景気警戒タブ |
| economic_indicators | 72 | 米国景気警戒タブ（employment_monthlyから変換） |
| market_state_history | 192 | 統合タブ |
| layer_stress_history | 576 | 統合タブ |
| stock_master | 23 | 銘柄マスター（初期データ投入） |
| **合計** | **23,853** | |

### 7.3 stock_master 初期データ（23銘柄）

| カテゴリ | 銘柄 |
|---------|------|
| **penny** (<$20) | SOUN, RGTI, IONQ, SOFI, GSAT, MARA, PLUG, NIO |
| **mid** ($20-100) | COIN, RKLB, PLTR, HOOD, AFRM, SNAP, ROKU |
| **large** ($100+) | NVDA, TSLA, META, AAPL, MSFT, GOOGL, AMZN, AMD |

### 7.4 移行対象外テーブル

| テーブル | 理由 |
|---------|------|
| holdings | RLS有効・user_id必須（ユーザー登録後に手動） |
| trades | RLS有効・user_id必須（ユーザー登録後に手動） |
| user_watchlists | RLS有効・user_id必須（ユーザー登録後に手動） |
| user_settings | RLS有効・user_id必須（ユーザー登録後に手動） |
| stock_cache | 一時キャッシュ（実行時自動生成） |
| economic_indicator_revisions | 修正履歴用（今後追加） |

---

## 8. Railway バックエンド（実装済み）

### 8.1 デプロイ情報

| 項目 | 値 |
|------|-----|
| **URL** | https://empathetic-hope-production.up.railway.app |
| **プロジェクト名** | clever-spontaneity |
| **リージョン** | US East (Virginia) |
| **ポート** | 8000 |

### 8.2 環境変数（Railway Variables）

| Key | Value | 備考 |
|-----|-------|------|
| `SUPABASE_URL` | https://xndbmsrscozqyksstzop.supabase.co | |
| `SUPABASE_KEY` | ※service_role キー | RLSバイパス（2026-02-25追加） |
| `SUPABASE_ANON_KEY` | ※anon キー | フォールバック用 |
| `ENVIRONMENT` | development | |

**注意**: `main.py` は `SUPABASE_KEY`（service_role）を優先し、なければ `SUPABASE_ANON_KEY` にフォールバック。

### 8.3 実装済みAPI（19個）

| エンドポイント | 説明 | 状態 |
|--------------|------|------|
| `GET /` | ルート（バージョン情報） | ✅ |
| `GET /health` | ヘルスチェック | ✅ |
| `GET /api/stocks` | stock_master一覧（23銘柄） | ✅ |
| `GET /api/stocks/{ticker}` | 特定銘柄詳細 | ✅ |
| `GET /api/signal/{ticker}` | V10シグナル計算 | ✅ ⚠️簡易版 |
| `GET /api/regime` | Market Regime判定 | ✅ |
| `GET /api/liquidity/overview` | 配管タブ概要 | ✅ |
| `GET /api/liquidity/fed-balance-sheet` | FRBバランスシート履歴 | ✅ |
| `GET /api/liquidity/interest-rates` | 金利履歴 | ✅ |
| `GET /api/liquidity/credit-spreads` | クレジットスプレッド履歴 | ✅ |
| `GET /api/liquidity/market-indicators` | 市場指標履歴 | ✅ |
| `GET /api/employment/overview` | 景気警戒タブ概要 | ✅ |
| `GET /api/employment/indicators` | 経済指標履歴 | ✅ |
| `GET /api/employment/weekly-claims` | 週次失業保険履歴 | ✅ |
| `GET /api/market-state` | 統合タブ履歴 | ✅ |
| `GET /api/market-state/latest` | 最新市場状態 | ✅ |
| `GET/POST/PUT/DELETE /api/holdings` | 保有銘柄CRUD | ✅ |
| `GET/POST/DELETE /api/trades` | 取引履歴CRUD | ✅ |
| `POST /api/trades/sell-from-holding` | 保有から売却（PnL自動計算） | ✅ |
| `GET /api/exit/{ticker}` | 5層Exit判定 | ✅ ⚠️簡易版 |
| `GET /api/exit/{ticker}/quick` | 簡易Exit判定（8%ストップ） | ✅ |
| `GET /api/stock/{ticker}` | 株価情報（5分キャッシュ） | ✅ |
| `GET /api/stock/{ticker}/quote` | 株価クオート（軽量版） | ✅ |
| `GET /api/stock/{ticker}/history` | OHLCV履歴 | ✅ |
| `GET /api/stock/{ticker}/ema` | EMA値 | ✅ |
| `POST /api/stock/batch` | 複数銘柄一括取得 | ✅ |
| `GET /api/stock/batch-quotes` | 複数銘柄一括取得(GET/キャッシュ対応) | ✅ |
| `GET /api/holdings/portfolio-history` | 日次ポートフォリオ推移 | ✅ |
| `POST /api/holdings/sell` | 保有銘柄売却（PnL自動計算） | ✅ |
| `GET/POST/PUT/DELETE /api/holdings/cash` | 現金残高CRUD | ✅ |
| `GET /api/fx/usdjpy` | USD/JPYライブレート | ✅ |

### 8.4 計算ロジックの移植状況

| ロジック | demo元ファイル | Railway状態 | 備考 |
|---------|--------------|-------------|------|
| V10シグナル | `combined_entry_detector.py` | ✅ 移植済み | EMA収束閾値（Regime別）、株価カテゴリ別RS閾値対応 |
| BOS検出 | `bos_detector.py` | ✅ 移植済み | Grade判定（EXTENSION/REVERSAL/CONTINUATION） |
| CHoCH検出 | `choch_detector.py` | ✅ 移植済み | Quality判定（EXCELLENT/GOOD/WEAK/INVALID）、連続カウント |
| Exit判定 | `exit_manager.py` | ✅ 移植済み | 5層Exit System完全版 |
| Regime判定 | `regime_detector.py` | ✅ 移植済み | 4Regime（BULL/WEAKENING/BEAR/RECOVERY） |
| 資産クラス | `asset_class.py` | ✅ 移植済み | US/JP/Crypto対応、設定パラメータ |

**移植完了（2026-02-19）:**
- 全ての本格ロジックがdemo/analysis/からapp/backend/analysis/に移植済み
- signal.py, regime.pyルーターが本格ロジックを使用するように更新済み

### 8.5 ディレクトリ構成（現在）

```
app/backend/
├── main.py              # FastAPIエントリーポイント（SUPABASE_KEY優先）
├── auth.py              # Cloudflare Access認証 (require_auth)
├── precomputed.py       # 事前計算結果の高速読み取り
├── requirements.txt     # Python依存関係
├── Dockerfile           # コンテナ定義（Python 3.11-slim）
├── railway.json         # Railway設定
├── .env.example         # 環境変数テンプレート
├── .gitignore
├── routers/
│   ├── __init__.py
│   ├── stocks.py        # /api/stocks
│   ├── signal.py        # /api/signal/{ticker}
│   ├── regime.py        # /api/regime
│   ├── liquidity.py     # /api/liquidity（事前計算高速パス付き）
│   ├── employment.py    # /api/employment（事前計算高速パス付き）
│   ├── market_state.py  # /api/market-state
│   ├── holdings.py      # /api/holdings（CF Access認証）
│   ├── trades.py        # /api/trades（CF Access認証）
│   ├── exit.py          # /api/exit
│   └── stock.py         # /api/stock
└── analysis/
    ├── __init__.py
    └── exit_manager.py  # 5層Exit System
```

### 8.6 デプロイ時の注意点（トラブルシューティング）

| 問題 | 解決策 |
|------|--------|
| httpxバージョン競合 | requirements.txtからhttpxを削除（supabaseが自動インストール） |
| supabase proxy エラー | supabase==2.10.0 以上を使用 |
| yfinanceがブロックされる | yfinance>=0.2.40 を使用 |
| ヘルスチェック失敗 | railway.jsonからhealthcheckPathを削除、UI設定もクリア |
| RLSでデータ取得不可 | 公開テーブルはRLS無効化（DISABLE ROW LEVEL SECURITY） |

### 8.7 RLS設定（Supabase）— 2026-02-25 全テーブルRLS有効化

**アーキテクチャ:**
- バックエンド (Railway): `service_role` キー → RLSバイパス（全操作可）
- バッチ (ローカル): `service_role` キー → RLSバイパス（全操作可）
- 直接PostgRESTアクセス: `anon` キー → RLS適用（SELECTのみ）

**全テーブル RLS有効 + anonポリシー:**

| テーブル | RLS | anonポリシー | 備考 |
|---------|-----|-------------|------|
| fed_balance_sheet | 有効 | `anon_select` (SELECT) | バッチ書込み |
| interest_rates | 有効 | `anon_select` (SELECT) | バッチ書込み |
| credit_spreads | 有効 | `anon_select` (SELECT) | バッチ書込み |
| market_indicators | 有効 | `anon_select` (SELECT) | バッチ書込み |
| bank_sector | 有効 | `anon_select` (SELECT) | バッチ書込み |
| srf_usage | 有効 | `anon_select` (SELECT) | バッチ書込み |
| margin_debt | 有効 | `anon_select` (SELECT) | バッチ書込み |
| mmf_assets | 有効 | `anon_select` (SELECT) | バッチ書込み |
| economic_indicators | 有効 | `anon_select` (SELECT) | バッチ書込み |
| economic_indicator_revisions | 有効 | `anon_select` (SELECT) | バッチ書込み |
| weekly_claims | 有効 | `anon_select` (SELECT) | バッチ書込み |
| layer_stress_history | 有効 | `anon_select` (SELECT) | バッチ書込み |
| market_state_history | 有効 | `anon_select` (SELECT) | バッチ書込み |
| data_revisions | 有効 | `anon_select` (SELECT) | バッチ書込み |
| stock_master | 有効 | `anon_select` (SELECT) | 読取専用 |
| stock_cache | 有効 | `anon_select` (SELECT) | キャッシュ |
| manual_inputs | 有効 | `anon_select` (SELECT) | バッチ書込み |
| precomputed_results | 有効 | `anon_select` (SELECT) | 事前計算結果 |
| holdings | 有効 | `anon_select` (SELECT) | service_roleでCRUD |
| trades | 有効 | `anon_select` (SELECT) | service_roleでCRUD |
| user_settings | 有効 | `anon_select` (SELECT) | service_roleでCRUD |
| user_watchlists | 有効 | `anon_select` (SELECT) | service_roleでCRUD |
| cash_balances | 有効 | `anon_select` (SELECT) | service_roleでCRUD |
| portfolio_snapshots | 有効 | `anon_select` (SELECT) | バッチ書込み |

**DB構造変更 (2026-02-25):**
- `holdings.user_id`: UUID → TEXT（Cloudflare Accessメール対応）
- `trades.user_id`: UUID → TEXT（同上）
- `auth.users` への外部キー制約を削除（Supabase Auth未使用）

**SQLファイル:**
- `app/batch/sql/setup_all.sql` — 14テーブルのRLS + ポリシー
- `app/batch/sql/fix_rls_missing_tables.sql` — 残り5テーブル + user_id修正 + precomputed_results作成

---

## 10. フロントエンド実装（2026-02-20〜22）

### 10.1 デプロイ情報

| 項目 | 値 |
|------|-----|
| **フレームワーク** | Next.js 15.2 (App Router) |
| **スタイル** | Tailwind CSS + shadcn/ui |
| **ビルド** | `output: 'export'`（静的エクスポート） |
| **画像** | `images: { unoptimized: true }`（Cloudflare Pages対応） |
| **Turbopack** | 無効（日本語パス名の問題で webpack 使用） |

### 10.2 デザインシステム — Bloomberg Terminal 風グラスモーフィズム

配管（Liquidity）ページを起点に、Bloomberg Terminal風のダークテーマ+グラスモーフィズムデザインを全ページに適用。

**グローバルCSS（globals.css）で定義済みの `plumb-*` クラス:**

| クラス | 説明 |
|--------|------|
| `plumb-glass` | `backdrop-blur` + 半透明背景のグラスカード |
| `plumb-animate-in` | フェードイン + スライドアップアニメーション |
| `plumb-stagger-1` 〜 `plumb-stagger-6` | 段階的遅延アニメーション |
| `plumb-gradient-border` | グラデーションボーダー |
| `plumb-glow` | 光彩効果 |
| `plumb-tabs` | shadcn Tabs 用カスタムスタイル |

### 10.3 共有コンポーネント

**`src/components/shared/glass.tsx`**
配管ページから抽出した再利用可能コンポーネント:
- `GlassCard` — グラスモーフィズムカード
- `ScoreRing` — SVG円形スコア表示
- `GaugeBar` — ゲージバー
- `Metric` — ラベル+値の表示
- `StatusChip` — ステータスバッジ
- `ScoreLegend` — スコア凡例
- `DocSection` / `DocTable` — ドキュメント表示用アコーディオン
- `scoreHue()`, `scoreBarClass()`, `scoreLabel()` — ヘルパー関数

**`src/components/shared/TickerIcon.tsx`**
- CDN（`cdn.jsdelivr.net/gh/nvstly/icons/ticker_icons/{SYMBOL}.png`）から企業ロゴ取得
- 取得失敗時はハッシュベース12色パレットの略称文字にフォールバック
- サイズ可変（18px〜72px）

### 10.4 チャートコンポーネント（Canvas描画）

**`src/components/charts/CandlestickChart.tsx`**
- ローソク足チャート（OHLCV）
- 下部にボリュームバー（緑=上昇/赤=下降、全体の18%）
- EMA8（緑実線）/ EMA21（赤破線）
- BOS / CHoCH / FVG マーカー表示対応
- 現在価格ライン表示

**`src/components/charts/LineChartCanvas.tsx`**
- ラインチャート（終値のみ）
- 価格エリアがフルハイト（ボリュームなし）
- EMA8 / EMA21 表示
- グラデーション塗りつぶし

### 10.5 実装済みページ

#### 統合ダッシュボード（`/` トップページ）
- demoの `integrated.html`（2800行）からReact/Next.jsに完全移行
- 2タブ構成: **ダッシュボード** / **システム解説**
- **ダッシュボード タブ:**
  - 統合インサイトHero: State×Phase組み合わせから動的メッセージ生成（6パターン）
  - 2カラムシステムカード（左: 流動性配管 / 右: 米国景気警戒）各ScoreRing + GaugeBar + 詳細リンク
  - State×Phase マトリクス（5×5テーブル、現在位置ハイライト、投資アドバイス）
  - 動的インサイトカード（Policy Regime、各レイヤー異常値、サームルール発動）
  - ナビゲーションカード（4ページへの導線）
- **システム解説 タブ:** DocSection×6（ダッシュボードの使い方、流動性配管とは、景気警戒とは、マトリクスの読み方、過去の危機事例、注意事項）
- APIフック5つ使用: usePlumbingSummary, useEmploymentRiskScore, useMarketEvents, usePolicyRegime, useRegime

#### 配管タブ（`/liquidity`）
- 3層流動性ストレスモニター（Layer 1: FRB / Layer 2A: 銀行 / Layer 2B: リスク選好）
- Market State ヒーロー + ScoreRing
- 市場指標（VIX / DXY / S&P500 / NASDAQ）
- クレジット圧力センサー
- ネット流動性フロー
- システム解説タブ（DocSection/DocTable）

#### シグナルタブ（`/signals`）
- Bloomberg Terminal 風グラスモーフィズムにリデザイン済み
- 運用モードセレクター（Conservative / Balanced / Aggressive）
- ティッカー検索 + クイックティッカー（TickerIcon付き）
- ヒーローカード: TickerIcon(72px) + ティッカー + 価格 + 変動率
- shadcn Tabs で4タブ:
  - **エントリー判定**: BUY/NO ENTRYヒーロー + 条件カード3列グリッド + RS + モード比較
  - **保有分析**: EXIT/HOLDヒーロー + 5層Exit判定カード + ターゲット価格
  - **過去シグナル**: 統計サマリー4列 + タイムライン（ENTRY=blue/EXIT=red/HEAT=orange/RSI_HIGH=purple）
  - **システム解説**: DocSection/DocTableでV10ロジック説明
- バッチ分析: 全銘柄一括シグナル取得
- チャート: ローソク足/ライン切替 + 期間選択(1M/3M/6M/1Y) + EMA/BOS/CHoCH/FVGトグル

#### 保有タブ（`/holdings`）
- 保有銘柄CRUD

#### 雇用タブ（`/employment`）
- 米国景気警戒指標
- EconomicPhaseHero に総合ScoreRing（size=88）追加: 3カテゴリ（雇用/消費者/構造）の横に総合スコアを大きく表示

### 10.6 追加API（V2エンドポイント）

フロントエンド開発に伴い、バックエンドに以下のV2エンドポイントを追加:

| エンドポイント | 説明 |
|--------------|------|
| `GET /api/v2/signals/{ticker}` | V10シグナル（エントリー+Exit+チャートデータ統合） |
| `POST /api/v2/signals/batch` | 複数銘柄一括シグナル分析 |
| `GET /api/v2/signals/{ticker}/history` | シグナル履歴タイムライン（ENTRY/EXIT/RSI_HIGH） |
| `GET /api/v2/chart-markers/{ticker}` | BOS/CHoCH/FVG チャートマーカー |

### 10.7 Favicon

- App Router metadata方式（`icon.tsx` / `apple-icon.tsx`）
- カスタムロゴ画像から生成

### 10.8 フロントエンド ディレクトリ構成

```
app/frontend/
├── next.config.ts
├── package.json
├── tailwind.config.ts
├── .env.local                          # API_URL設定
├── public/
├── src/
│   ├── app/
│   │   ├── layout.tsx                  # ルートレイアウト（サイドナビ）
│   │   ├── page.tsx                    # トップ（統合タブ）
│   │   ├── globals.css                 # plumb-* グローバルCSS
│   │   ├── icon.tsx                    # Favicon
│   │   ├── apple-icon.tsx              # Apple Touch Icon
│   │   ├── liquidity/page.tsx          # 配管タブ
│   │   ├── signals/page.tsx            # シグナルタブ
│   │   ├── holdings/page.tsx           # 保有タブ
│   │   └── employment/page.tsx         # 雇用タブ
│   ├── components/
│   │   ├── shared/
│   │   │   ├── glass.tsx               # 共有グラスモーフィズムコンポーネント
│   │   │   └── TickerIcon.tsx          # 企業ロゴ/略称アイコン
│   │   ├── charts/
│   │   │   ├── CandlestickChart.tsx    # ローソク足（+ Volume）
│   │   │   └── LineChartCanvas.tsx     # ラインチャート
│   │   └── ui/                         # shadcn/ui コンポーネント
│   ├── lib/
│   │   ├── api.ts                    # API関数 + SWRフック15個
│   │   ├── swr.tsx                   # SWRグローバル設定プロバイダー
│   │   └── utils.ts
│   └── types/
│       └── index.ts                    # BOSMarker, CHoCHMarker, FVGMarker 型定義
```

---

## 12. Cloudflare Pages デプロイ（2026-02-23）

### 12.1 デプロイ情報

| 項目 | 値 |
|------|-----|
| **URL** | https://open-regime.pages.dev/ |
| **プロジェクト名** | open-regime |
| **デプロイ方式** | Wrangler CLI 手動デプロイ（Git連携なし） |
| **アクセス制限** | Cloudflare Access（Zero Trust）メールOTP認証 |

### 12.2 デプロイ手順

```bash
# フロントエンドをビルド＆デプロイ（1コマンド）
cd app/frontend && npm run build && wrangler pages deploy out --project-name=open-regime --commit-dirty=true
```

### 12.3 アクセス制限（Cloudflare Access）

| 設定 | 値 |
|------|-----|
| **Zero Trust チーム** | urbansea.cloudflareaccess.com |
| **Application** | open-regime（Self-hosted） |
| **保護ドメイン** | `open-regime.pages.dev` + `*.open-regime.pages.dev` |
| **Policy** | allowed-users（Allow、Emails指定） |
| **認証方式** | One-time PIN（メールOTP）+ Google OAuth |

**ユーザー追加方法:**
1. https://one.dash.cloudflare.com/ → Access → Applications → open-regime
2. Policies → allowed-users を編集
3. Include → Emails にメールアドレスを追加 → Save

### 12.4 CORS設定

Backend（`app/backend/main.py`）で `https://open-regime.pages.dev` を許可済み。

### 12.5 Git連携について

GitHub リポジトリ名 `-` が Cloudflare Pages のGit連携で認識されないため、手動デプロイを採用。
リポジトリ名を変更すればGit連携（`git push` で自動デプロイ）が可能。

### 12.6 注意事項

- フロントエンド更新時は `wrangler pages deploy` が必要（Backendは `git push` で自動）
- Cloudflare Access の設定変更は Zero Trust ダッシュボードから（https://one.dash.cloudflare.com/）
- Pages Settings の「Access policy」はプレビューデプロイメントのみ対象。本番保護は Zero Trust 側で設定

---

## 13. パフォーマンス改善（2026-02-25実施）

### 13.1 SWR導入（フロントエンド）

**概要**: `useSWR` を全ページに導入し、2回目以降のページ表示を0msに。

**変更ファイル:**
| ファイル | 内容 |
|---------|------|
| `app/frontend/src/lib/swr.tsx` | SWRグローバル設定プロバイダー |
| `app/frontend/src/lib/api.ts` | SWRフック15個追加 |
| `app/frontend/src/app/layout.tsx` | `<SWRProvider>` ラッパー追加 |
| `app/frontend/src/app/page.tsx` | Server Component → Client + useSWR |
| `app/frontend/src/app/liquidity/page.tsx` | useEffect → useSWR (3箇所) |
| `app/frontend/src/app/employment/page.tsx` | useEffect → useSWR (2箇所) |
| `app/frontend/src/app/holdings/page.tsx` | useEffect → useSWR (4箇所) |
| `app/frontend/src/app/signals/page.tsx` | 初期データのみuseSWR (2箇所) |

**SWRグローバル設定:**
- `revalidateOnFocus: false` — フォーカス時の自動再取得を無効
- `dedupingInterval: 2000ms` — 重複リクエスト防止
- `errorRetryCount: 2` — エラー時リトライ回数

**SWR化しないもの（ユーザートリガー系）:**
- `getSignal()`, `getExitAnalysis()`, `getBatchSignals()` — ボタン押下で明示的に実行するため

### 13.2 バッチ事前計算（バックエンド + バッチ）

**概要**: 重い4エンドポイントの計算結果をSupabaseに事前保存し、APIはDB直読みで応答。

**テーブル:** `precomputed_results`
```sql
CREATE TABLE precomputed_results (
    key TEXT PRIMARY KEY,          -- "risk_score", "plumbing_summary", "market_events", "policy_regime"
    result JSONB NOT NULL,         -- 計算結果JSON
    computed_at TIMESTAMPTZ NOT NULL DEFAULT NOW()
);
```

**対象エンドポイント:**
| キー | エンドポイント | 改善 |
|------|-------------|------|
| `risk_score` | `/api/employment/risk-score` | 500ms → ~50ms |
| `plumbing_summary` | `/api/liquidity/plumbing-summary` | 800ms → ~50ms |
| `market_events` | `/api/liquidity/events` | 400ms → ~50ms |
| `policy_regime` | `/api/liquidity/policy-regime` | 500ms → ~50ms |

**フロー:**
```
バッチ (--precompute / --daily / --weekly)
  → バックエンドAPIをHTTP呼び出し
  → レスポンスJSONをprecomputed_resultsにupsert

バックエンドAPI呼び出し時:
  1. precomputed_resultsから読み取り（24時間TTL）
  2. 有効 → DB結果をそのまま返す（~50ms）
  3. 期限切れ/なし → 従来のフル計算にフォールバック
```

**変更ファイル:**
| ファイル | 内容 |
|---------|------|
| `app/batch/calculators/precompute.py` | API→Supabase事前計算保存 |
| `app/batch/run.py` | `--precompute` フラグ追加、daily/weekly末尾に自動実行 |
| `app/backend/precomputed.py` | 共有ヘルパー `get_precomputed()` |
| `app/backend/routers/employment.py` | risk-score高速パス追加 |
| `app/backend/routers/liquidity.py` | plumbing/events/policy高速パス追加 |

### 13.3 Cloudflare Workers Edge プロキシ（2026-02-25実施）

**概要**: 日本→US East (Railway) のTCP+TLSハンドシェイクが~800ms。Workers を東京PoP に配置し、Cache API で日次データを ~60ms で配信。

**Worker URL**: `https://open-regime-api.ryu3ta-ke-mo100307.workers.dev`

**ファイル構成:**

| ファイル | 内容 |
|---------|------|
| `app/worker/wrangler.jsonc` | Worker設定 (name, vars: ORIGIN, ALLOWED_ORIGIN) |
| `app/worker/package.json` | 最小依存 (wrangler, typescript) |
| `app/worker/tsconfig.json` | Workers用TS設定 |
| `app/worker/src/index.ts` | メインプロキシロジック |
| `app/worker/src/cache-config.ts` | エンドポイント別TTL設定 |

**キャッシュ戦略（TTL）:**

| エンドポイント | TTL | 理由 |
|---------------|-----|------|
| `/api/liquidity/overview` | **24時間** | 日次バッチデータ（次回バッチまで保持） |
| `/api/liquidity/plumbing-summary` | **24時間** | 同上 |
| `/api/liquidity/events` | **24時間** | 同上 |
| `/api/liquidity/policy-regime` | **24時間** | 同上 |
| `/api/liquidity/history-charts` | **24時間** | 同上 |
| `/api/liquidity/backtest-states` | **24時間** | 同上 |
| `/api/employment/risk-score` | **24時間** | 同上 |
| `/api/employment/risk-history` | **24時間** | 同上 |
| `/api/employment/overview` | **24時間** | 同上 |
| `/api/stocks` | **24時間** | マスターデータ |
| `/api/regime` | **5分** | yfinance EMA都度計算（シグナルタブでSPY価格表示に使用） |
| `/api/market-state/latest` | **5分** | 市場データ連動 |
| `/api/fx/usdjpy` | **5分** | 為替レート |
| `/api/stock/batch-quotes` | **5分** | yfinance都度取得 |
| `/api/holdings/init` | **1分** | ユーザー個別（per-userキャッシュキー） |
| `/api/holdings` | **1分** | 同上 |
| `/api/holdings/cash` | **1分** | 同上 |
| `/api/holdings/portfolio-history` | **2分** | 同上 |
| `/api/holdings/*` (CRUD), `/api/trades/*` | **なし** | ユーザーCRUD |
| `/api/signal/*`, `/api/stock/*`, `/api/exit/*` | **なし** | リアルタイム/オンデマンド |

**処理フロー:**
```
1. OPTIONSプリフライト → CORS応答
2. /api/*以外 → 404
3. キャッシュ対象 & GETなら → cache.match()
4. ヒット → CORS付きで返却 (X-Cache: HIT)
5. ミス → Railwayにプロキシ
6. レスポンス200 & キャッシュ対象 → cache.put() (waitUntil)
7. CORS付きで返却 (X-Cache: MISS)
```

**パフォーマンス実測（2026-02-25）:**

| エンドポイント | Worker (cached) | 直接 Railway | 改善 |
|--|--|--|--|
| `/api/regime` | **64ms** | 975ms | **15倍** |
| `/api/employment/risk-score` | **69ms** | 2,651ms | **38倍** |
| `/api/liquidity/plumbing-summary` | **59ms** | 1,477ms | **25倍** |
| `/api/employment/risk-history` | **53ms** | 1,036ms | **20倍** |

**キャッシュウォームアップ:**

バッチ完了後にWorker全エンドポイントを1回叩いてキャッシュを充填する `_warmup_cache()` を `app/batch/run.py` に追加。daily/weekly/precompute 実行後に自動実行される。これにより朝一番のユーザーもキャッシュ HIT で高速表示。

**フロントエンド変更:**
- `app/frontend/.env.local`: `NEXT_PUBLIC_API_URL` を Worker URL に変更
- `app/frontend/src/lib/api.ts` / `swr.tsx`: 変更なし（環境変数経由で自動切替）

**バックエンド変更:**
- `app/backend/main.py`: CORS `allow_origins` に Worker URL を追加

---

## 14. CI/CD — GitHub Actions 自動デプロイ（2026-02-25実施）

### 14.1 概要

`git push` to main で Worker + Pages を自動デプロイ。バックエンド (Railway) は従来通り Railway 自身が自動デプロイ。

### 14.2 ワークフロー

**ファイル:** `.github/workflows/deploy.yml`

**トリガー:** `push` to `main` (paths: `app/worker/**` or `app/frontend/**`)

**ジョブ:**

| ジョブ | 条件 | 処理 |
|--------|------|------|
| `changes` | 常に実行 | `dorny/paths-filter` で変更パスを検出 |
| `deploy-worker` | `app/worker/**` 変更時 | `npm ci` → `wrangler deploy` |
| `deploy-pages` | `app/frontend/**` 変更時 | `npm ci` → `next build` → `wrangler pages deploy` |

### 14.3 必要なSecrets

| Secret | 値 | 設定場所 |
|--------|-----|---------|
| `CLOUDFLARE_API_TOKEN` | Cloudflare API Token (`github-actions-deploy-cloudflare`) | GitHub Settings → Secrets |
| `CLOUDFLARE_ACCOUNT_ID` | `c1a8026d00eb674a5d2750d6ef1527ba` | GitHub Settings → Secrets |

**Cloudflare API Token 権限:**
- Workers Scripts: Edit
- Cloudflare Pages: Edit
- Account Settings: Read

### 14.4 デプロイフロー全体図

```
git push to main
  │
  ├── Railway: バックエンド自動デプロイ（Railway側のGit連携）
  │
  └── GitHub Actions (.github/workflows/deploy.yml)
       ├── app/worker/** 変更 → Cloudflare Worker デプロイ (~25秒)
       └── app/frontend/** 変更 → Next.js ビルド → Cloudflare Pages デプロイ
```

### 14.5 手動デプロイ（フォールバック）

```bash
# Worker
cd app/worker && npx wrangler deploy

# Frontend
cd app/frontend && npm run build && wrangler pages deploy out --project-name=open-regime --commit-dirty=true
```

### 14.6 日次バッチ自動実行（cron）

**ファイル:** `.github/workflows/batch-daily.yml`

**トリガー:** `schedule` (cron: `30 22 * * *` = 毎朝 7:30 JST) + `workflow_dispatch`（手動実行）

**処理フロー:**
```
毎朝 7:30 JST (22:30 UTC)
  └── GitHub Actions (ubuntu-latest, Python 3.12)
       ├── pip install -r app/backend/requirements.txt
       └── python app/batch/run.py --daily --verbose
            ├── FRED/NY Fed: 3年分取得（修正検知）
            ├── Yahoo: 直近14日取得
            ├── Supabase upsert
            ├── precompute_all() → 重い計算結果をDB保存
            └── _warmup_cache() → Worker全12エンドポイントのキャッシュ充填
```

**追加 Secrets（バッチ用）:**

| Secret | 用途 |
|--------|------|
| `SUPABASE_URL` | Supabase プロジェクトURL |
| `SUPABASE_KEY` | service_role キー（RLSバイパス） |
| `FRED_API_KEY` | FRED APIキー |

**タイムアウト:** 15分（通常は2-5分で完了）

### 14.7 Git連携について

GitHub リポジトリ名 `-` が Cloudflare のネイティブ Git 連携で認識されないため、GitHub Actions で代替。
リポジトリ名を変更すればネイティブ連携も可能。

---

## 15. 日次ポートフォリオスナップショット（2026-02-25実施）

### 15.1 概要

保有銘柄の時価を毎日の終値で評価し、`portfolio_snapshots` テーブルに保存するシステム。
資産推移タブのデータソースとなる。

**なぜ必要か:**
- 取引履歴だけでは「取得原価」しかわからない
- 株価の日々の変動（含み損益の推移）を追跡するには、毎日のスナップショットが必須

### 15.2 データフロー

```
日次バッチ (_run_daily)
  ├── FRED/Yahoo/NY Fed データ取得 → Supabase upsert
  ├── fetch_market_indicators → upsert （USD/JPY がここで保存）
  ├── fetch_bank_sector → upsert
  └── take_daily_snapshot()  ← ここで実行
        ├── holdings テーブル → 全ユーザーの保有銘柄取得
        ├── yf.download(全ティッカー, period="5d") → 終値取得（1回のAPI呼び出し）
        ├── market_indicators → USD/JPY レート取得
        ├── cash_balances → 全ユーザーの現金残高取得
        └── ユーザーごとにスナップショット行構築 → portfolio_snapshots に upsert
```

### 15.3 スナップショット行の内容

| フィールド | 説明 |
|-----------|------|
| `total_market_value_usd` | Σ(shares × close_price) — 時価評価額 |
| `total_cost_usd` | Σ(shares × avg_price) — 取得原価 |
| `unrealized_pnl_usd` | market_value - cost — 含み損益 |
| `cash_usd` | USD現金 + JPY現金/fx_rate — 現金合計(USD換算) |
| `total_assets_usd` | market_value + cash — 総資産 |
| `fx_rate_usdjpy` | 換算に使ったレート |
| `holdings_detail` | 銘柄別詳細JSONB（ticker, shares, close_price, market_value等） |

### 15.4 バッチモジュール

| ファイル | 役割 |
|---------|------|
| `app/batch/snapshot.py` | 日次スナップショット（`take_daily_snapshot()`） |
| `app/batch/backfill_snapshots.py` | 過去データバックフィル（取引履歴から再構築） |
| `app/batch/db.py` | `upsert_portfolio_snapshots()` |
| `app/batch/run.py` | `--snapshot` / `--backfill-snapshots` フラグ |

### 15.5 過去データバックフィル

取引履歴から過去のポートフォリオ状態を日ごとに再構築:

```
python app/batch/run.py --backfill-snapshots --since 2024-06-01 --verbose
```

1. 全ユーザーの trades を trade_date ASC で取得
2. 日付ごとに holdings_state を更新（BUY: 追加, SELL: 減少）
3. `yf.download(all_tickers, start=since, end=today)` で過去終値を一括取得
4. 営業日ごとにスナップショット行を生成 → upsert

**注意:** バックフィル分は `cash_usd=0` （過去の現金推移は不明のため）

### 15.6 APIエンドポイント

```
GET /api/holdings/portfolio-history?months=24
```

`portfolio_snapshots` テーブルからユーザーの日次スナップショットを返却。
レスポンス:
```json
{
  "history": [
    {
      "date": "2025-01-15",
      "total_market_value_usd": 12500.00,
      "total_cost_usd": 10000.00,
      "unrealized_pnl_usd": 2500.00,
      "cash_usd": 5000.00,
      "total_assets_usd": 17500.00,
      "holdings_count": 5
    }
  ],
  "summary": { ... }
}
```

---

## 16. 現金残高管理（2026-02-25実施）

### 16.1 概要

`cash_balances` テーブルで証券口座・銀行口座の現金残高を管理。
ポートフォリオスナップショットの `cash_usd` に自動反映される。

### 16.2 API

| エンドポイント | 説明 |
|--------------|------|
| `GET /api/holdings/cash` | 現金残高一覧 |
| `POST /api/holdings/cash` | 現金残高追加 |
| `PUT /api/holdings/cash/{id}` | 現金残高更新 |
| `DELETE /api/holdings/cash/{id}` | 現金残高削除 |

### 16.3 通貨換算

- JPY → USD: `amount / fx_rate`
- USD → USD: そのまま
- `fx_rate` は `market_indicators` テーブルから最新の USD/JPY を使用

---

## 17. DB容量管理・スケーリング戦略

### 17.1 現在の容量見積もり

| テーブル | 1行あたり | 行数/日 | 年間行数 | 年間容量 |
|---------|----------|---------|---------|---------|
| portfolio_snapshots | ~1KB (JSONB含む) | ユーザー数 | ~365×N | ~365KB×N |
| market_indicators | ~100B | 1 | ~250 | ~25KB |
| economic_indicators | ~100B | ~20 | ~5,000 | ~500KB |
| fed_balance_sheet | ~80B | 1 | ~250 | ~20KB |

**ユーザー5人の場合:** portfolio_snapshots = 年間 ~1.8MB
**Supabase無料枠:** 500MB（累計、年リセットなし）

### 17.2 スケーリング戦略

**Phase 1（現在〜500MB以内）:**
- 日次スナップショット（毎営業日）
- 何も変更不要

**Phase 2（容量が増えてきたら）:**
- スナップショット頻度を**2週間に1回**に変更
  - `app/batch/run.py` の `take_daily_snapshot()` 呼び出しに曜日条件を追加
  - 例: `if datetime.now().weekday() == 4:` (毎週金曜のみ)
- 古いスナップショットを間引き（月初のみ保持）
  - `DELETE FROM portfolio_snapshots WHERE snapshot_date < '2024-01-01' AND EXTRACT(DAY FROM snapshot_date) != 1`

**Phase 3（500MB超え/有料化検討時）:**
- Supabase Pro ($25/月, 8GB)
- または holdings_detail JSONB を別テーブルに分離（本体テーブルを軽量化）
- パーティショニング（年別テーブル分割）

### 17.3 容量モニタリング

Supabase ダッシュボード → Database → Disk Usage で確認可能。
300MBを超えたら間引き戦略の導入を検討。

---

## 11. 次にやること（TODO）

### 11.1 優先度：高

| タスク | 説明 | 状態 |
|--------|------|------|
| **本格ロジック移植** | demo/analysis/* → backend/analysis/ に移植 | ✅ 完了 |
| ├── combined_entry_detector.py | V10本体（EMA収束閾値、株価カテゴリ別RS閾値） | ✅ |
| ├── bos_detector.py | BOS Grade判定（EXTENSION/REVERSAL/CONTINUATION） | ✅ |
| ├── choch_detector.py | CHoCH検出（Bullish/Bearish、Quality判定） | ✅ |
| ├── regime_detector.py | 4Regime判定（BULL/WEAKENING/BEAR/RECOVERY） | ✅ |
| └── asset_class.py | 資産クラス定義（US/JP/Crypto対応） | ✅ |
| **フロントエンド開発** | Next.js + Cloudflare Pages | ✅ 主要ページ完了 |
| ├── 配管タブ | liquidity API連携、グラスモーフィズムデザイン | ✅ |
| ├── シグナルタブ | V2 signal API連携、Bloomberg Terminal風リデザイン | ✅ |
| ├── 保有タブ | holdings/trades API連携 | ✅ |
| ├── 雇用タブ | employment API連携 | ✅ |
| ├── 共有コンポーネント | glass.tsx, TickerIcon.tsx 抽出 | ✅ |
| ├── チャート | CandlestickChart(+Vol), LineChartCanvas | ✅ |
| └── Favicon | カスタムロゴ設定 | ✅ |

### 11.2 完了済み（インフラ）

| タスク | 完了日 |
|--------|--------|
| Cloudflare Pagesデプロイ | 2026-02-23 |
| Workers Edgeプロキシ | 2026-02-25 |
| GitHub Actions CI/CD | 2026-02-25 |
| バッチcron自動化 | 2026-02-25 |

### 11.3 優先度：高 — UI/UX改善

#### A. 統合タブ（トップページ）強化 ✅ 完了（2026-02-26）

demoの `integrated.html` から完全移行。264行→~480行にリライト。
- 統合インサイトHero（State×Phase動的メッセージ生成）
- 2カラムシステムカード（配管+景気警戒、各ScoreRing+GaugeBar+詳細リンク）
- State×Phase 5×5マトリクス（現在位置ハイライト）
- 動的インサイトカード（Policy Regime, レイヤー異常値, サームルール）
- ナビゲーションカード（全4ページへ）
- システム解説タブ（DocSection×6、初心者向けガイド）
- APIフック5つ: usePlumbingSummary, useEmploymentRiskScore, useMarketEvents, usePolicyRegime, useRegime

#### B. 保有タブ改修 ✅ 完了（2026-02-25）

| 項目 | 状態 |
|------|------|
| トレード入力UI | ✅ 新規登録モーダル（ticker, price, shares, account, sector, thesis） |
| トレード編集/削除 | ✅ 編集モーダル + 削除確認 + 売却モーダル（売却理由・学び・P&Lプレビュー） |
| ポートフォリオ分析 | ✅ セクター/口座/ティッカー別ドーナツチャート3種 |
| 損益推移グラフ | ✅ AssetTrendsTab: Canvas時系列チャート（総資産/時価/原価/含み損益）、JPY/USD切替 |
| エグジットシグナル連携 | ✅ `/signals?ticker=X` へのリンク（エントリー/履歴/Exit分析） |
| 現金残高管理 | ✅ CashBalanceSection: JPY/USD対応、口座別、inline編集 |
| バックエンドAPI | ✅ CRUD全実装済み |

#### C. オンボーディング（初回ユーザーガイド） ✅ 完了（2026-02-26）

| 項目 | 実装 |
|------|------|
| ウェルカムモーダル | ✅ `WelcomeModal.tsx` — 5ステップDialogガイド、localStorage `onboarding_done` フラグ管理 |
| 用語解説パネル | ✅ `GlossaryPanel.tsx` — ヘッダー「?」アイコン → 右スライドパネル、27用語×4カテゴリ、ページ別フィルタ |
| 用語データ | ✅ `glossary-data.ts` — liquidity/employment/signals/general、pageGlossaryMapでルート別表示 |
| スキップ可能 | ✅ 「次回から表示しない」チェックボックス + 設定ページからガイド再表示 |

#### D. ユーザーUI ✅ 完了（2026-02-26）

| 項目 | 実装 |
|------|------|
| ユーザープロバイダー | ✅ `UserProvider.tsx` — `/cdn-cgi/access/get-identity`からメール取得、`useUser()`フック提供 |
| ユーザーメニュー | ✅ `UserMenu.tsx` — イニシャルアバター + ドロップダウン（メール表示、設定、ログアウト） |
| ログアウト | ✅ `/cdn-cgi/access/logout` にリダイレクト |
| 設定ページ | ✅ `/settings` — アカウント情報、テーマ切替、ガイド再表示、ログアウト |

### 11.4 優先度：中 — 機能拡張

| タスク | 状態 | 説明 | 詳細 |
|--------|------|------|------|
| **stock_cache活用** | ✅ 完了（2026-02-26） | yfinance結果をDBキャッシュ | `cache_utils.py`新規作成。`signal.py`4箇所+`combined_entry_detector.py`のyfinance直接呼び出し→`fetch_ohlcv_cached()`に統一。L2 DBキャッシュ（TTL=300s）でMISS時のみyfinance→DB保存。`stock_cache.ticker`をVARCHAR(30)に拡張（キー形式: `ohlcv:{ticker}:{period}`） |
| **シグナルタブに日本株追加** | ✅ 完了（2026-02-26） | 日本株対応 | 日本株ティッカー（7203等）のyfinance取得・エントリー判定対応。クイックティッカーにJPプリセット8銘柄追加（トヨタ/ソフトバンクG/ソニーG/三菱UFJ/キーエンス/任天堂/ファストリ/信越化学）。JPバッジ表示、¥通貨記号切替 |
| **シグナルタブ保有分析改修** | 未着手 | 保有分析タブの改善 | 現在の保有分析タブの表示・機能を見直し。改善点は精査の上で決定 |
| **シグナルタブUI改善** | ✅ 完了（2026-02-26） | 米国景気警戒タブ風デザインに統一 | ページラッパー`pb-10`統一、ヘッダー`flex-col sm:flex-row`統一、カードパディング`p-4→p-5`・`space-y-3→space-y-4`統一、ヒーローカードGlassCardラップ+パディング統一、チャートセクションパディング統一 |
| **ポートフォリオ配分を国別表示** | ✅ 完了（2026-02-26） | 国別ドーナツチャート追加 | `inferCountry()`でJP/US自動判定（`/^\d+$/`→JP）。国別ドーナツチャート（米国株=青`#3b82f6`/日本株=赤`#ef4444`）を保有ページに追加。セクター/国別/銘柄別の3ドーナツ表示 |
| **過去リスクスコア修正** | ✅ 完了（2026-02-26） | ズーム初期化+S&P500修正 | S&P500オーバーレイが途中で切れる問題を修正: ①`market_indicators`データを1999年まで遡及取得（7,080行バックフィル）、②Supabase PostgREST 1000行制限を`.range()`ページネーションで解消。日本株通貨記号修正: シグナルタブ全体で`$`→`¥`動的切替（`/^\d/.test(ticker)`でJP判定） |
| **通知（Slack/LINE）** | 未着手 | シグナル発生・リスク変動の自動通知 | バッチ後にリスクスコア変動チェック→閾値超えでWebhook送信。Slack Incoming Webhook or LINE Notify。エントリーシグナル発生時も通知対象 |
| **weeklyバッチcron** | ❌ 不要 | ~~週次バッチも自動化~~ | 日次バッチ（GitHub Actions schedule 毎朝7:30 JST）で全データソースを取得済み。FRBバランスシート等も日次で更新されるため、週次専用cronは不要と判断 |
| **配管タブ履歴チャートCanvas化** | ✅ 完了（2026-02-26） | Recharts→Canvas移行 | `liquidity/page.tsx`の7チャート（net_liquidity, margin_debt, kre, spreads, vix, layer_scores, divergence）を`EconChartCanvas`に置換。`toSeries()`/`mergeOverlay()`ヘルパー追加。Rechartsインポート全削除（バンドルサイズ: 16.5kB）。ズーム/パン/S&P500・NASDAQオーバーレイ対応 |

### 11.5 優先度：低 — 将来/商用化

| タスク | 状態 | 説明 | 詳細 |
|--------|------|------|------|
| **バックテスト機能** | 未着手 | シグナル精度検証 | 過去N年分のエントリーシグナルを再計算→仮想トレード→勝率/PF/最大DD計算。`/api/backtest/{ticker}?start=...&end=...`新規API |
| **モバイル対応** | 未着手 | レスポンシブUI | 現在デスクトップ最適化のみ。Tailwindの`sm:`/`md:`ブレークポイント追加。テーブル→カードビュー切替。チャートはタッチ対応済み（Canvas） |
| **マルチテナント** | 未着手 | 組織アクセス管理 | Cloudflare Access のグループ機能活用。org_id概念追加→holdings/tradesをorg単位で共有可能に。※個人利用では不要 |
| **課金/サブスク** | 未着手 | Stripe連携 | Free: 統合+配管閲覧のみ、Pro: 全機能。Stripe Checkout→Webhook→`user_plans`テーブル→API側でプランチェック。※個人利用では不要 |
| **データエクスポート** | 未着手 | CSV/PDF出力 | 保有一覧、取引履歴、リスクスコア履歴をCSV/PDFでダウンロード。フロントエンド側で生成（`papaparse` + `jspdf`） |
| **ウォッチリスト** | ✅ 完了（2026-02-26） | 銘柄監視リスト | `user_watchlists`テーブル+CRUD API（`watchlist.py`）実装。シグナルページのクイックティッカーをバックエンド永続化。認証済→API同期、未認証→localStorage fallback。初回認証時にlocalStorage→バックエンド自動マイグレーション。Worker cache-configにno-cacheルール追加 |

---

## 18. 統合ダッシュボード移行 + デザイン統一（2026-02-26実施）

### 18.1 統合ダッシュボード移行

**概要**: demoの `integrated.html`（2800行 Flask/Jinja2）を Next.js の `page.tsx` に完全移行。264行の簡素な3カードレイアウトから、~480行の本格統合ダッシュボードにリライト。

**移行元**: `demo/dashboard/templates/integrated.html`
**移行先**: `app/frontend/src/app/page.tsx`

**機能一覧:**

| セクション | 内容 |
|-----------|------|
| 統合インサイトHero | State(配管)×Phase(景気)の組み合わせから動的メッセージ生成（6パターン）、背景色はリスクレベルに応じて変化 |
| 2カラムシステムカード | 左: 流動性配管（MarketState + L1/L2A/L2B ScoreRing + GaugeBar + イベント一覧）、右: 米国景気警戒（Phase + 総合ScoreRing + 3カテゴリScoreRing + アラート一覧） |
| State×Phase マトリクス | 5×5テーブル（行=配管State、列=景気Phase）、各セルに投資アドバイス、現在位置を青枠+glowでハイライト |
| 動的インサイトカード | Policy Regime、各レイヤーの異常値、サームルール発動を条件に応じて表示 |
| ナビゲーションカード | 流動性配管/米国景気警戒/シグナル分析/保有管理の4ページへの導線カード |
| システム解説タブ | DocSection×6（使い方、流動性配管とは、景気警戒とは、マトリクスの読み方、過去の危機事例、注意事項） |

**インサイト生成ロジック（State×Phase → メッセージ）:**

| 条件 | メインメッセージ |
|------|----------------|
| LIQUIDITY_SHOCK + CRISIS/CONTRACTION | 両システムが危険 - フルキャッシュ推奨 |
| LIQUIDITY_SHOCK or CRISIS/CONTRACTION | 一方が危険 - 大幅リスク縮小 |
| (TIGHTENING/CONTRACTION) + (CAUTION/SLOWDOWN) | 両方警戒 - 守り重視 |
| (TIGHTENING/CONTRACTION) or (CAUTION/SLOWDOWN) | 一方が警戒 - 慎重に |
| HEALTHY + EXPANSION | 両方安全 - 通常投資可能 |
| default | 様子見が適切 |

**マトリクスデータ（5×5）:**

| | EXPANSION | SLOWDOWN | CAUTION | CONTRACTION | CRISIS |
|---|---|---|---|---|---|
| **HEALTHY** | 積極投資OK | 慎重に継続 | 利確検討 | ポジション縮小 | 利確急ぐ |
| **NEUTRAL** | 通常投資 | 様子見 | 新規控え | 防御的に | 大幅縮小 |
| **TIGHTENING** | 選別投資 | 新規控え | 守り重視 | リスク縮小 | キャッシュ寄せ |
| **CONTRACTION** | ポジション縮小 | 守り重視 | 大幅縮小 | キャッシュ確保 | フルキャッシュ |
| **LIQ. SHOCK** | キャッシュ寄せ | 大幅縮小 | フルキャッシュ | フルキャッシュ | フルキャッシュ |

### 18.2 雇用タブ ScoreRing追加

**変更**: `app/frontend/src/app/employment/page.tsx`
- EconomicPhaseHero に総合ScoreRing（size=88, strokeWidth=6）を追加
- 3カテゴリ（雇用/消費者/構造）の左に大きな総合スコアリングを配置

### 18.3 デザイン統一 — ページタイトルグラデーションバー

全ページのタイトル横にグラデーション縦バー（`w-1.5 h-6 rounded-full bg-gradient-to-b`）を統一配置。

| ページ | グラデーション色 |
|--------|----------------|
| 統合ダッシュボード (`/`) | `from-blue-500 to-emerald-500` |
| 流動性配管 (`/liquidity`) | `from-blue-500 to-purple-500`（既存） |
| 米国景気警戒 (`/employment`) | `from-blue-500 to-orange-500`（既存） |
| シグナル分析 (`/signals`) | `from-blue-500 via-cyan-400 to-purple-500`（横線→縦線に変更） |

---

## 19. Admin ダッシュボード（2026-02-27 実施）

### 19.1 概要

管理者専用の Admin Dashboard を **別プロジェクトの Cloudflare Pages** として作成。ユーザー向けフロントエンドとは完全に分離し、独立したデプロイ・認証を実現。

**ドメイン**: `open-regime-admin.pages.dev`
**ソースコード**: `app/admin-frontend/`（Next.js 15 + Tailwind + shadcn/ui）

### 19.2 認証（3層構造）

| 層 | 仕組み | 説明 |
|----|--------|------|
| 第1層 | Cloudflare Access (OAuth) | サイト全体を保護。許可メール以外はアクセス不可 |
| 第2層 | `ADMIN_EMAILS` 環境変数 | Railway 環境変数に登録されたメールのみ API 許可。非管理者には 404 を返す（存在を隠す） |
| 第3層 | TOTP MFA | Google Authenticator 等の 6桁コードで二要素認証（セクション 20 で詳述） |

### 19.3 Backend API（`app/backend/routers/admin.py`）

全エンドポイントは `require_admin_mfa` 依存で保護。

| メソッド | パス | 説明 |
|---------|------|------|
| GET | `/api/admin/users` | 全ユーザー一覧（id, email, display_name, plan, auth_provider, is_active, last_login_at, created_at） |
| PATCH | `/api/admin/users/{user_id}` | ユーザーの plan / display_name / is_active 更新 |
| GET | `/api/admin/stats` | ユーザー統計（総数, 7日/30日アクティブ, 今月新規, 日別登録数グラフ用データ） |
| GET | `/api/admin/audit-logs` | 監査ログ一覧（管理者の操作履歴） |
| GET | `/api/admin/batch-logs` | バッチ実行ログ一覧 |
| GET | `/api/admin/feature-flags` | 機能フラグ一覧 |
| POST | `/api/admin/feature-flags` | 機能フラグ作成 |
| PATCH | `/api/admin/feature-flags/{flag_id}` | 機能フラグ ON/OFF 切替 |

### 19.4 DB テーブル（`app/batch/sql/setup_admin.sql`）

```sql
-- 監査ログ
admin_audit_logs (id, admin_user_id, action, target_type, target_id, old_value JSONB, new_value JSONB, created_at)

-- バッチ実行ログ
batch_logs (id, batch_name, status, started_at, finished_at, records_processed, error_message, metadata JSONB)

-- 機能フラグ
feature_flags (id, flag_key UNIQUE, description, enabled, created_at, updated_at)
```

### 19.5 Frontend 構成（`app/admin-frontend/src/`）

| ファイル | 役割 |
|---------|------|
| `app/page.tsx` | メインダッシュボード（タブ: ユーザー管理/統計/監査ログ/バッチログ/機能フラグ） |
| `app/layout.tsx` | "Open Regime Admin" ヘッダー、グラスモーフィズムデザイン |
| `components/providers/UserProvider.tsx` | CF Access get-identity でメール取得 |
| `components/shared/glass.tsx` | GlassCard 等の共有コンポーネント |
| `lib/api.ts` | Admin API クライアント（`X-User-Email` + `X-MFA-Token` ヘッダー付与） |
| `lib/swr.tsx` | SWR fetcher（`X-MFA-Token` ヘッダー対応） |
| `lib/auth-store.ts` | モジュールレベル email 保持 |

### 19.6 Worker 対応

`app/worker/src/index.ts` の `ALLOWED_ORIGIN` 環境変数に admin ドメインを追加。Worker が admin からの API リクエストを Backend に転送。

### 19.7 CI/CD

`.github/workflows/deploy.yml` に `deploy-admin` ジョブ追加。`app/admin-frontend/**` の変更を検出して自動デプロイ（`open-regime-admin` Pages プロジェクト）。

---

## 20. Admin TOTP MFA（2026-02-28 実施）

### 20.1 概要

Admin ダッシュボードに TOTP（Time-based One-Time Password）による多要素認証を追加。Google Authenticator 等の Authenticator アプリで 6桁コードを生成し、ログイン時に入力。

### 20.2 認証フロー

```
初回セットアップ:
  管理者ログイン → MfaGate(state=setup) → MfaSetup表示
    → QRコード表示（pyotp + qrcode）
    → Authenticatorアプリでスキャン
    → 6桁コード入力で確認
    → MFA有効化 + セッショントークン発行
    → ダッシュボード表示

以降のログイン:
  管理者ログイン → MfaGate(state=challenge) → MfaChallenge表示
    → 6桁コード入力
    → セッショントークン発行（1時間有効）
    → ダッシュボード表示

ログアウト時:
  clearMfaToken() → localStorage クリア → CF Access ログアウト
  → 次回ログイン時にコード再入力必須
```

### 20.3 Backend MFA API（`app/backend/routers/admin_mfa.py`）

| メソッド | パス | 認証 | 説明 |
|---------|------|------|------|
| GET | `/api/admin/mfa/status` | require_admin | MFA 設定状態（mfa_enabled, mfa_setup） |
| POST | `/api/admin/mfa/setup` | require_admin | TOTP シークレット生成 + QR コード（base64 PNG） |
| POST | `/api/admin/mfa/setup/verify` | require_admin | セットアップ確認 → MFA 有効化 + トークン発行 |
| POST | `/api/admin/mfa/verify` | require_admin | ログイン時 TOTP 検証 → トークン発行 |
| GET | `/api/admin/mfa/session` | require_admin | セッショントークン有効性検証 |

### 20.4 セキュリティ設計

| 項目 | 実装 |
|------|------|
| TOTP シークレット | `pyotp.random_base32()` — `admin_mfa.secret_enc` に保存 |
| セッショントークン | `secrets.token_hex(32)` (256bit) |
| トークン保存 | SHA-256 ハッシュを `admin_mfa_sessions.token_hash` に保存（平文は DB に保存しない） |
| セッション有効期限 | 1時間（`_SESSION_DURATION_HOURS = 1`） |
| TOTP 許容窓 | `valid_window=1`（±30秒） |
| QR コード発行者名 | `OpenRegimeAdmin` |
| RLS | `admin_mfa`, `admin_mfa_sessions` ともに RLS 有効（service_role のみアクセス） |

### 20.5 DB テーブル（`app/batch/sql/setup_mfa.sql`）

```sql
-- MFA シークレット管理
CREATE TABLE admin_mfa (
    id          SERIAL PRIMARY KEY,
    user_id     UUID NOT NULL UNIQUE,
    secret_enc  TEXT NOT NULL,        -- TOTP シークレット（Base32）
    enabled     BOOLEAN NOT NULL DEFAULT FALSE,
    created_at  TIMESTAMPTZ NOT NULL DEFAULT NOW(),
    updated_at  TIMESTAMPTZ NOT NULL DEFAULT NOW()
);
ALTER TABLE admin_mfa ENABLE ROW LEVEL SECURITY;

-- MFA セッショントークン
CREATE TABLE admin_mfa_sessions (
    id          SERIAL PRIMARY KEY,
    user_id     UUID NOT NULL,
    token_hash  TEXT NOT NULL,        -- SHA-256 ハッシュ
    expires_at  TIMESTAMPTZ NOT NULL,
    created_at  TIMESTAMPTZ NOT NULL DEFAULT NOW()
);
CREATE INDEX idx_admin_mfa_sessions_user ON admin_mfa_sessions (user_id);
CREATE INDEX idx_admin_mfa_sessions_hash ON admin_mfa_sessions (token_hash);
ALTER TABLE admin_mfa_sessions ENABLE ROW LEVEL SECURITY;
```

### 20.6 Backend 依存追加（`app/backend/requirements.txt`）

```
pyotp==2.9.0          # TOTP 生成・検証
qrcode[pil]==8.0      # QR コード画像生成
```

### 20.7 全エンドポイントの MFA 保護

`app/backend/routers/admin.py` に `require_admin_mfa` 関数を追加。`require_admin` → MFA 設定状態チェック → 有効なら `X-MFA-Token` ヘッダーのセッション検証。MFA 未設定の管理者はそのまま通過（段階的導入対応）。

全 Admin API エンドポイント（users, stats, audit-logs, batch-logs, feature-flags）を `Depends(require_admin)` → `Depends(require_admin_mfa)` に切替。

### 20.8 Worker 対応

`app/worker/src/index.ts`:
- CORS `Access-Control-Allow-Headers` に `X-MFA-Token` 追加
- 信頼 Origin からのみ `X-MFA-Token` ヘッダーを Backend に転送

### 20.9 Frontend MFA コンポーネント

| ファイル | 役割 |
|---------|------|
| `components/mfa/MfaGate.tsx` | MFA ゲートラッパー（状態: loading → setup / challenge / authenticated） |
| `components/mfa/MfaSetup.tsx` | QR コード表示 + マニュアルシークレット表示 + 6桁コード入力（セットアップ検証） |
| `components/mfa/MfaChallenge.tsx` | 6桁コード入力 UI（オートフォーカス、Enter キー対応） |
| `lib/mfa-store.ts` | localStorage トークン管理（`getMfaToken`, `setMfaToken`, `clearMfaToken`、有効期限チェック） |

`app/page.tsx` のダッシュボードコンテンツを `<MfaGate>` でラップ。ログアウト時に `clearMfaToken()` を呼び出し、次回ログイン時にコード再入力を強制。

---

## 更新履歴

| 日付 | 内容 |
|------|------|
| 2026-02-19 | 初版作成（設計議論の結果をまとめ） |
| 2026-02-19 | DB設計を大幅更新: 19テーブル構成に変更、配管/景気警戒/統合タブ用テーブル追加、経済指標を縦持ち方式（無限修正履歴対応）に変更、不要なシグナル関連テーブルを削除 |
| 2026-02-19 | Supabase移行完了: 23,853件のデータ移行、stock_master初期データ23銘柄投入 |
| 2026-02-19 | Railwayバックエンドデプロイ完了: FastAPI 14エンドポイント実装、全API動作確認済み |
| 2026-02-19 | 追加API実装: market-state, holdings, trades, exit, stock（計19 API稼働） |
| 2026-02-19 | RLS無効化: holdings, trades, user_watchlists, user_settings, economic_indicator_revisions, stock_cache |
| 2026-02-19 | holdings/trades APIを設計ドキュメント準拠に修正（avg_price, action, trade_date等）|
| 2026-02-19 | 本格ロジック移植完了: combined_entry_detector(V10), bos_detector, choch_detector, regime_detector, asset_class をdemoから移植、signal.py/regime.pyを本格版に更新 |
| 2026-02-20 | フロントエンド開発開始: Next.js 15.2 + Tailwind + shadcn/ui、配管タブ（Bloomberg Terminal風グラスモーフィズム）実装 |
| 2026-02-20 | 共有コンポーネント抽出: glass.tsx（GlassCard, ScoreRing, GaugeBar等）、plumb-* CSSクラス体系構築 |
| 2026-02-21 | シグナルタブ リデザイン: グラスモーフィズム統一、4タブ（エントリー/保有/過去シグナル/システム解説）、V2 API連携 |
| 2026-02-21 | TickerIcon コンポーネント作成: jsDelivr CDNから企業ロゴ取得、フォールバック付き |
| 2026-02-21 | チャートコンポーネント: CandlestickChart（+Volume）、LineChartCanvas、BOS/CHoCH/FVGマーカー対応 |
| 2026-02-21 | V2 APIエンドポイント追加: signals/{ticker}, signals/batch, signals/{ticker}/history, chart-markers/{ticker} |
| 2026-02-22 | チャート調整: ローソク足にVolume表示、ラインチャートはフルハイト（Volumeなし）、TickerIconサイズ72px |
| 2026-02-22 | Favicon設定: カスタムロゴからApp Router metadata方式で生成 |
| 2026-02-22 | セクション10（フロントエンド実装）追加、TODO更新 |
| 2026-02-23 | Cloudflare Pages移行完了: `open-regime.pages.dev` にデプロイ、Cloudflare Access（Zero Trust）でメール認証によるアクセス制限設定、CORS設定更新 |
| 2026-02-25 | SWR導入: 全ページに`useSWR`フック15個追加、2回目以降のページ表示0ms |
| 2026-02-25 | バッチ事前計算: `precomputed_results`テーブル追加、4 heavyエンドポイントをDB直読み化（500ms→50ms） |
| 2026-02-25 | RLS全テーブル有効化: 全20+テーブルにRLS+ポリシー設定、service_roleキーに切替 |
| 2026-02-25 | DB構造修正: holdings/tradesの`user_id`をUUID→TEXT、auth.users FK制約削除（Cloudflare Accessメール対応） |
| 2026-02-25 | Railway環境変数追加: `SUPABASE_KEY`（service_role）、`main.py`でservice_role優先に変更 |
| 2026-02-25 | Cloudflare Workers Edgeプロキシ: 東京PoPで API キャッシュ、15-38倍高速化（~60ms）、エンドポイント別TTL（5分〜4時間） |
| 2026-02-25 | GitHub Actions CI/CD: `dorny/paths-filter`でWorker/Pages自動デプロイ、手動デプロイ不要に |
| 2026-02-25 | バッチにキャッシュウォームアップ追加: `_warmup_cache()` でバッチ後にWorker全エンドポイントを事前キャッシュ |
| 2026-02-25 | 日次バッチcron自動化: GitHub Actions schedule (毎朝7:30 JST)、workflow_dispatch対応 |
| 2026-02-25 | 環境変数の`.strip()`追加: GitHub Secrets改行混入対策（`config.py`の`SUPABASE_URL`/`SUPABASE_KEY`/`FRED_API_KEY`） |
| 2026-02-25 | 保有タブ改修: 株価リアルタイム表示（GET batch-quotes + Worker 5分キャッシュ）、スケルトンローディング、SWR導入 |
| 2026-02-25 | 現金残高管理: `cash_balances`テーブル追加、CRUD API + UI実装、ポートフォリオ総資産に現金反映 |
| 2026-02-25 | 日次ポートフォリオスナップショット: `portfolio_snapshots`テーブル追加、`batch/snapshot.py`（日次）+ `batch/backfill_snapshots.py`（過去バックフィル）、資産推移タブを時価ベースに更新 |
| 2026-02-25 | DB容量管理・スケーリング戦略セクション追加（Section 17） |
| 2026-02-25 | 保有タブにTickerIconとシグナル連携アクション追加 |
| 2026-02-26 | 統合ダッシュボード移行: demoの`integrated.html`(2800行)からNext.js `page.tsx`に完全移行（264行→~480行）。State×Phaseマトリクス、動的インサイト生成、2カラムシステムカード、初心者向けシステム解説タブ |
| 2026-02-26 | 雇用タブ: EconomicPhaseHeroに総合ScoreRing（size=88）追加 |
| 2026-02-26 | デザイン統一: 全ページのタイトルにグラデーション縦バー（`w-1.5 h-6 bg-gradient-to-b`）を統一配置。シグナルタブは横線→縦線に変更 |
| 2026-02-26 | セクション18（統合ダッシュボード移行+デザイン統一）追加、TODO 11.3.A を完了マーク |
| 2026-02-26 | Onboarding実装: `/api/user/me`エンドポイント、`useUser` SWRフック、`UserGreeting`コンポーネント（Cf-Access-Authenticated-User-Email対応）。TODO 11.3.C 完了 |
| 2026-02-26 | User UI実装: `/settings`ページ（保有一覧/取引履歴/アカウント情報）、ユーザーメニュー（アバター+ドロップダウン）をヘッダー右上に追加。TODO 11.3.D 完了 |
| 2026-02-26 | stock_cache活用: `cache_utils.py`新規作成、`signal.py`4箇所+`combined_entry_detector.py`のyfinance呼び出しをL2 DBキャッシュ化（TTL=300s）。`stock_cache.ticker`をVARCHAR(30)に拡張。TODO 11.4 stock_cache 完了 |
| 2026-02-26 | 配管Canvas化: `liquidity/page.tsx`の7チャートをRecharts→EconChartCanvasに置換。`toSeries()`/`mergeOverlay()`ヘルパー追加、バンドルサイズ削減。TODO 11.4 Canvas化 完了 |
| 2026-02-26 | ウォッチリスト: `watchlist.py` CRUD API + `user_watchlists`テーブル、フロントエンドSWR同期（認証時API/未認証時localStorage）、初回マイグレーション。TODO 11.5 ウォッチリスト 完了 |
| 2026-02-26 | TODO 11.4 weeklyバッチcronを「不要」に変更（日次バッチで全データ取得済み） |
| 2026-02-26 | シグナルタブUI改善: 米国景気警戒タブ風デザインに統一（`pb-10`、`flex-col sm:flex-row`ヘッダー、`p-4→p-5`、`space-y-3→space-y-4`）。TODO 11.4 シグナルタブUI改善 完了 |
| 2026-02-26 | シグナルタブ日本株対応: JPクイックティッカー8銘柄追加、JPバッジ・¥通貨記号切替。TODO 11.4 日本株追加 完了 |
| 2026-02-26 | ポートフォリオ国別配分: `inferCountry()`でJP/US自動判定、国別ドーナツチャート追加。TODO 11.4 ポートフォリオ配分 完了 |
| 2026-02-26 | S&P500データ修正: `market_indicators`を1999年まで遡及バックフィル（7,080行追加）。`employment.py`の`fetch_market()`をSupabase `.range()`ページネーションに変更（PostgREST 1000行上限対応）。S&P500オーバーレイが2014年以降のみ表示→全期間表示に修正。TODO 11.4 過去リスクスコア修正 完了 |
| 2026-02-26 | 日本株通貨記号修正: `signals/page.tsx`で`$`ハードコード→`isJP`/`ccy`ヘルパーで`¥`動的切替。保有分析・エグジット分析・EMAステータス・ストラクチャストップ・ターゲット価格・過去シグナル履歴の全箇所を対応 |
| 2026-02-27 | Worker Cache TTL最適化: 日次バッチデータ（liquidity全6 + employment全3 + stocks）のTTLを15分〜4時間→**24時間**に統一。バッチ→ウォームアップ→次回バッチまでキャッシュHIT維持。`/api/regime`は yfinance EMA都度計算のため5分据置。保有タブはper-userキャッシュ（1-2分）で前回最適化済み |
| 2026-02-27 | ウォームアップパラメータ修正: `_warmup_cache()`の`risk-history?months=120`→`months=350`に修正（フロントエンドのデフォルト値と一致させ、キャッシュMISS解消） |
| 2026-02-27 | Admin ダッシュボード作成: `app/admin-frontend/`（Next.js 15 + Tailwind + shadcn/ui）を別 Cloudflare Pages プロジェクト (`open-regime-admin.pages.dev`) としてデプロイ。Backend に Admin API 8エンドポイント（users, stats, audit-logs, batch-logs, feature-flags）+ 監査ログ + 機能フラグ管理。Cloudflare Access + ADMIN_EMAILS による2層認証。CI/CD（GitHub Actions `deploy-admin` ジョブ）追加。セクション19 追加 |
| 2026-02-28 | Admin TOTP MFA 実装: Google Authenticator 対応の多要素認証。Backend に `admin_mfa.py`（5エンドポイント: status/setup/setup-verify/verify/session）。DB に `admin_mfa` + `admin_mfa_sessions` テーブル。`pyotp==2.9.0` + `qrcode[pil]==8.0` 追加。全 Admin API を `require_admin_mfa` に切替。Worker に `X-MFA-Token` ヘッダー転送追加。Frontend に MfaGate/MfaSetup/MfaChallenge コンポーネント + mfa-store（localStorage）。セッション1時間、ログアウト時クリア。セクション20 追加 |
| 2026-02-28 | Supabase Auth 移行: Cloudflare Access → Supabase Auth（Email/Password + Google OAuth）。Backend に JWT dual-mode 認証（`Authorization: Bearer` 優先 + レガシー `X-User-Email` フォールバック）。Worker に `Authorization` ヘッダー転送追加。Frontend に `@supabase/supabase-js`（implicit flow）+ ログイン/登録/パスワードリセット/コールバック/メール確認ページ追加。AuthGuard で保護ページ（dashboard/liquidity/employment/signals/holdings/settings）をラップ。ランディングページを認証状態対応に改修（未認証: CTA表示、認証済み: ダッシュボードリンク）。ヘッダーを認証状態対応（未認証: ログインボタン、認証済み: フルナビ + UserMenu）。パスワード要件5項目（8文字以上/大文字/小文字/数字/記号）+ 表示トグル。GitHub Actions deploy.yml に Supabase 環境変数追加。Google OAuth 設定（Google Cloud Console + Supabase Provider）。セクション5.2 更新 |
| 2026-02-28 | JWT ES256 対応 + ログインループ修正: Supabase JWT が ES256 (ECDSA) 署名であることに対応。`auth.py` に `PyJWKClient` で JWKS 公開鍵検証を追加（HS256 フォールバック付き）。フロントエンドに 401 リダイレクトループ防止ガード（`_isRedirecting` フラグ）を `auth-store.ts` / `swr.tsx` / `api.ts` に追加。`/api/auth/check` JWT 診断エンドポイント追加。`email_confirmed_at` チェックを警告のみに緩和。favicon.ico を `icon.png` から再生成。セクション 5.2, 8.2 更新。詳細: `tasks/done/login-loop-fix-2026-02-28.md` |
